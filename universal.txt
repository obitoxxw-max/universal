-- ============================================================
-- AnonHub 
-- ============================================================

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")
local TeleportService = game:GetService("TeleportService")
local Lighting = game:GetService("Lighting")

UserInputService.MouseIconEnabled = true

local player = Players.LocalPlayer
local camera = Workspace.CurrentCamera

local defaultLighting = {
    Brightness = Lighting.Brightness,
    ClockTime = Lighting.ClockTime,
    FogEnd = Lighting.FogEnd,
    GlobalShadows = Lighting.GlobalShadows,
    OutdoorAmbient = Lighting.OutdoorAmbient
}

-- ============================================================
-- WINDUI LOADER
-- ============================================================
local WindUI

do
    local ok, result = pcall(function()
        return require("./src/Init")
    end)

    if ok then
        WindUI = result
    else
        if RunService:IsStudio() then
            WindUI = require(game:GetService("ReplicatedStorage"):WaitForChild("WindUI"):WaitForChild("Init"))
        else
            WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()
        end
    end
end

-- ============================================================
-- VARIABLES GLOBALES
-- ============================================================
local aimEnabled = false
local aimMode = "Hold" -- NOUVEAU : "Hold" ou "Toggle"
local aimToggledOn = false
local fovRadius = 150
local showFov = false
local smoothnessVal = 10
local checkWalls = true
local checkTeam = false
local lockedTarget = nil
local targetedPlayers = {}
local fovColor = Color3.fromRGB(255, 50, 50)

local espEnabled = false
local showNames = true
local showHealth = true
local espColor = Color3.fromRGB(0, 200, 255)
local espStyle = "Highlight"
local activeESPs = {}

local tracersEnabled = false
local tracerOrigin = "Bottom"

local crosshairEnabled = false
local crosshairStyle = "Cross"
local crosshairColor = Color3.fromRGB(0, 255, 0)
local crosshairThickness = 2
local crosshairGap = 5

local speedEnabled = false
local walkSpeedVal = 50
local flyEnabled = false
local flySpeedVal = 300
local noclipEnabled = false
local infJumpEnabled = false
local antiFlingEnabled = false

local antiAFK = false
local fullbright = false

local flyConn = nil
local currentAnimBundle = nil -- nil = animations d'origine du joueur conservées

-- WindUI keybinds & toggles (assigned during UI construction)
local aimKeybind = nil
local speedKeybind, flyKeybind, jumpKeybind, noclipKeybind = nil, nil, nil, nil
local speedToggle, flyToggle, infJumpToggle, noclipToggle = nil, nil, nil, nil
local refreshPlayerDropdown = nil
local homeTab = nil

-- ============================================================
-- REAL ANIMATION SYSTEM
-- ============================================================
local function ApplyAnimations(Animations, Character)
    local Animate = Character:FindFirstChild('Animate') or Character:WaitForChild('Animate', 5)
    if not Animate then return end
    for Animation, Ids in next, Animations do
        local AnimObject = Animate:FindFirstChild(Animation)
        if not AnimObject then continue end
        for i, id in next, Ids do
            local AnimationObject
            if Animation == 'idle' then
                AnimationObject = AnimObject:FindFirstChild('Animation'..tostring(i))
            elseif Animation == 'swim' and i == 2 then 
                local swimidle = Animate:FindFirstChild('swimidle')
                AnimationObject = swimidle and swimidle:FindFirstChild('SwimIdle')
            else
                local fullstr = string.upper(string.sub(Animation,1,1))..string.sub(Animation,2,#Animation)
                AnimationObject = AnimObject:FindFirstChild(fullstr..'Anim') or AnimObject:FindFirstChild(fullstr)
            end
            if AnimationObject and id then
                AnimationObject.AnimationId = "rbxassetid://"..tostring(id)
            end
        end
    end
end

local AnimationBundles = {
    ["Ninja"] = {idle = {656117400, 656118341}, walk = {656121766}, run = {656118852}, jump = {656117878}, fall = {656115606}, swim = {656119721, 656121397}, climb = {656114359}},
    ["Cartoony"] = {idle = {742637544, 742638445}, walk = {742640026}, run = {742638842}, jump = {742637942}, fall = {742637151}, swim = {742639220, 742639812}, climb = {742636889}},
    ["Stylish"] = {idle = {616136790, 616138447}, walk = {616146177}, run = {616140816}, jump = {616139451}, fall = {616134815}, swim = {616143378, 616144772}, climb = {616133594}},
    ["Zombie"] = {idle = {616158929, 616160636}, walk = {616168032}, run = {616163682}, jump = {616161997}, fall = {616157476}, swim = {616165109, 616166655}, climb = {616156119}},
    ["Mocap"] = {idle = {507766388, 507766951}, walk = {507777826}, run = {507767714}, jump = {507765000}, fall = {507767968}, swim = {507784897, 507785072}, climb = {507765644}}
}

local function SetAnimationBundle(bundleName)
    if bundleName == "Default" or not bundleName then
        currentAnimBundle = nil
        restoreDefaultAnimations()
        return
    end
    currentAnimBundle = bundleName
    local Character = player.Character
    if not Character then return end
    local bundle = AnimationBundles[bundleName]
    if not bundle then return end
    captureOriginalAnimations(Character)
    ApplyAnimations(bundle, Character)
end

-- Sauvegarde les animations d'origine du joueur (celles du jeu, pas celles du script)
local originalAnimations = {}

local function captureOriginalAnimations(Character)
    local Animate = Character and Character:FindFirstChild("Animate")
    if not Animate then return end
    for _, obj in ipairs(Animate:GetDescendants()) do
        if obj:IsA("Animation") and not originalAnimations[obj] then
            originalAnimations[obj] = obj.AnimationId
        end
    end
end

local function restoreDefaultAnimations()
    for obj, id in pairs(originalAnimations) do
        pcall(function()
            if obj.Parent then obj.AnimationId = id end
        end)
    end
    originalAnimations = {}
end

-- ============================================================
-- ROBUST FLING SYSTEM
-- ============================================================
local function SkidFling(TargetPlayer)
    local Character = player.Character
    local Humanoid = Character and Character:FindFirstChildOfClass("Humanoid")
    local RootPart = Humanoid and Humanoid.RootPart
    local TCharacter = TargetPlayer.Character
    if not TCharacter or not Character or not RootPart then return end
    
    local THumanoid = TCharacter:FindFirstChildOfClass("Humanoid")
    local TRootPart = THumanoid and THumanoid.RootPart
    if not THumanoid or not TRootPart or THumanoid.Sit then return end
    
    local OldPos = RootPart.CFrame
    local FPDH = Workspace.FallenPartsDestroyHeight
    Workspace.FallenPartsDestroyHeight = 0/0
    
    local BV = Instance.new("BodyVelocity")
    BV.Parent = RootPart
    BV.Velocity = Vector3.new(0, 0, 0)
    BV.MaxForce = Vector3.new(9e9, 9e9, 9e9)
    
    Humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, false)
    Character.PrimaryPart = RootPart
    
    local flingActive = true
    task.delay(2, function() flingActive = false end)
    
    local Angle = 0
    while flingActive and TCharacter and TCharacter.Parent and TRootPart do
        Angle = Angle + 100
        local pos1 = CFrame.new(TRootPart.Position) * CFrame.new(0, 1.5, 0) * CFrame.Angles(math.rad(Angle), 0, 0)
        RootPart.CFrame = pos1
        Character:SetPrimaryPartCFrame(pos1)
        RootPart.Velocity = Vector3.new(9e7, 9e8, 9e7)
        RootPart.RotVelocity = Vector3.new(9e8, 9e8, 9e8)
        task.wait()
        
        local pos2 = CFrame.new(TRootPart.Position) * CFrame.new(0, -1.5, 0) * CFrame.Angles(math.rad(Angle), 0, 0)
        RootPart.CFrame = pos2
        Character:SetPrimaryPartCFrame(pos2)
        RootPart.Velocity = Vector3.new(9e7, 9e8, 9e7)
        RootPart.RotVelocity = Vector3.new(9e8, 9e8, 9e8)
        task.wait()
    end
    
    BV:Destroy()
    Humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
    Workspace.FallenPartsDestroyHeight = FPDH
    
    RootPart.CFrame = OldPos * CFrame.new(0, 0.5, 0)
    Character:SetPrimaryPartCFrame(OldPos * CFrame.new(0, 0.5, 0))
    Humanoid:ChangeState(Enum.HumanoidStateType.GettingUp)
    for _, part in pairs(Character:GetChildren()) do
        if part:IsA("BasePart") then part.Velocity, part.RotVelocity = Vector3.new(), Vector3.new() end
    end
end

-- ============================================================
-- DRAWING API
-- ============================================================
local fovCircle = Drawing.new("Circle")
fovCircle.Visible = false
fovCircle.Thickness = 2
fovCircle.NumSides = 64
fovCircle.Filled = false
fovCircle.Transparency = 0.8
fovCircle.Color = fovColor
fovCircle.Radius = fovRadius

local crosshairDrawings = {
    top = Drawing.new("Line"), bottom = Drawing.new("Line"),
    left = Drawing.new("Line"), right = Drawing.new("Line"),
    dot = Drawing.new("Circle")
}

local playerVisuals = {}

local function getTracerOrigin()
    local size = camera.ViewportSize
    local mouse = UserInputService:GetMouseLocation()
    if tracerOrigin == "Bottom" then return Vector2.new(size.X/2, size.Y)
    elseif tracerOrigin == "Top" then return Vector2.new(size.X/2, 0)
    elseif tracerOrigin == "Center" then return size/2
    elseif tracerOrigin == "Mouse" then return mouse
    end
end

local function updateCrosshair(mousePos)
    if not crosshairEnabled then
        for _, l in pairs(crosshairDrawings) do l.Visible = false end
        UserInputService.MouseIconEnabled = true
        return
    end
    
    UserInputService.MouseIconEnabled = false
    
    local gap, size, thick, color = crosshairGap, 10, crosshairThickness, crosshairColor
    
    if crosshairStyle == "Cross" then
        crosshairDrawings.dot.Visible = false
        crosshairDrawings.top.Visible, crosshairDrawings.top.From, crosshairDrawings.top.To, crosshairDrawings.top.Color, crosshairDrawings.top.Thickness = true, Vector2.new(mousePos.X, mousePos.Y - gap), Vector2.new(mousePos.X, mousePos.Y - gap - size), color, thick
        crosshairDrawings.bottom.Visible, crosshairDrawings.bottom.From, crosshairDrawings.bottom.To, crosshairDrawings.bottom.Color, crosshairDrawings.bottom.Thickness = true, Vector2.new(mousePos.X, mousePos.Y + gap), Vector2.new(mousePos.X, mousePos.Y + gap + size), color, thick
        crosshairDrawings.left.Visible, crosshairDrawings.left.From, crosshairDrawings.left.To, crosshairDrawings.left.Color, crosshairDrawings.left.Thickness = true, Vector2.new(mousePos.X - gap, mousePos.Y), Vector2.new(mousePos.X - gap - size, mousePos.Y), color, thick
        crosshairDrawings.right.Visible, crosshairDrawings.right.From, crosshairDrawings.right.To, crosshairDrawings.right.Color, crosshairDrawings.right.Thickness = true, Vector2.new(mousePos.X + gap, mousePos.Y), Vector2.new(mousePos.X + gap + size, mousePos.Y), color, thick
    elseif crosshairStyle == "Circle" then
        for _, l in pairs(crosshairDrawings) do if typeof(l) == "table" or l.Name ~= "dot" then l.Visible = false end end
        crosshairDrawings.dot.Visible, crosshairDrawings.dot.Position, crosshairDrawings.dot.Radius, crosshairDrawings.dot.Color, crosshairDrawings.dot.Thickness, crosshairDrawings.dot.Filled = true, mousePos, gap + 5, color, thick, false
    elseif crosshairStyle == "Dot" then
        for _, l in pairs(crosshairDrawings) do if typeof(l) == "table" or l.Name ~= "dot" then l.Visible = false end end
        crosshairDrawings.dot.Visible, crosshairDrawings.dot.Position, crosshairDrawings.dot.Radius, crosshairDrawings.dot.Color, crosshairDrawings.dot.Thickness, crosshairDrawings.dot.Filled = true, mousePos, 2, color, 1, true
    end
end

-- ============================================================
-- AIMLOCK SYSTEM
-- ============================================================
local function getAimKeyEnum()
    if not aimKeybind or not aimKeybind.Value then return nil end
    local key = aimKeybind.Value
    if key == "MouseLeftButton" or key == "MouseLeft" then return Enum.UserInputType.MouseButton1 end
    if key == "MouseRightButton" or key == "MouseRight" then return Enum.UserInputType.MouseButton2 end
    if key == "None" or key == "" then return nil end
    return Enum.KeyCode[key]
end

local function isAimKeyHeld()
    local key = getAimKeyEnum()
    if not key then return false end
    if key.EnumType == Enum.KeyCode then return UserInputService:IsKeyDown(key) end
    return UserInputService:IsMouseButtonPressed(key)
end

-- ============================================================
-- ADVANCED ESP & VISUALS SYSTEM
-- ============================================================
local function cleanupESP(p)
    local data = activeESPs[p]
    if data then
        pcall(function()
            if data.Highlight then data.Highlight:Destroy() end
            if data.Billboard then data.Billboard:Destroy() end
            if data.SelectionBox then data.SelectionBox:Destroy() end
            if data.SkeletonLines then
                for _, lineData in pairs(data.SkeletonLines) do
                    if lineData.drawing and lineData.drawing.Remove then 
                        lineData.drawing:Remove() 
                    end
                end
            end
        end)
        activeESPs[p] = nil
    end
end

local function ensureESP(p)
    if p == player or not espEnabled then return end
    local char = p.Character
    if not char then return end
    
    local needsRecreation = false
    if activeESPs[p] then
        if activeESPs[p].Style ~= espStyle then
            needsRecreation = true
        end
    else
        needsRecreation = true
    end

    if needsRecreation then
        cleanupESP(p)
        local data = { Style = espStyle }
        
        local root = char:FindFirstChild("HumanoidRootPart")
        local head = char:FindFirstChild("Head")
        local hum = char:FindFirstChildOfClass("Humanoid")
        if root then
            local bb = Instance.new("BillboardGui")
            bb.Name = "AnonHub_Billboard_" .. p.Name
            bb.Adornee = head or root
            bb.Size = UDim2.new(0, 200, 0, 60)
            bb.StudsOffset = Vector3.new(0, 3.5, 0)
            bb.AlwaysOnTop = true
            bb.Enabled = true
            bb.Parent = char
            
            local nl = Instance.new("TextLabel")
            nl.Name = "NL"
            nl.Size = UDim2.new(1, 0, 0.5, 0)
            nl.BackgroundTransparency = 1
            nl.Text = p.Name
            nl.TextColor3 = espColor
            nl.TextSize = 16
            nl.Font = Enum.Font.GothamBold
            nl.TextStrokeTransparency = 0.6
            nl.Visible = showNames
            nl.Parent = bb
            
            local hlbl = Instance.new("TextLabel")
            hlbl.Name = "HL"
            hlbl.Size = UDim2.new(1, 0, 0.5, 0)
            hlbl.Position = UDim2.new(0, 0, 0.5, 0)
            hlbl.BackgroundTransparency = 1
            hlbl.Text = hum and math.floor(hum.Health).."/"..math.floor(hum.MaxHealth) or "100/100"
            hlbl.TextColor3 = espColor
            hlbl.TextSize = 14
            hlbl.Font = Enum.Font.Gotham
            hlbl.TextStrokeTransparency = 0.6
            hlbl.Visible = showHealth
            hlbl.Parent = bb
            data.Billboard = bb
        end

        if espStyle == "Highlight" then
            local hl = Instance.new("Highlight")
            hl.Adornee = char
            hl.FillColor = espColor
            hl.FillTransparency = 0.75
            hl.OutlineColor = espColor
            hl.OutlineTransparency = 0
            hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            hl.Parent = char
            hl.Enabled = true
            data.Highlight = hl
            
        elseif espStyle == "3D Box" then
            local sb = Instance.new("SelectionBox")
            sb.Adornee = char
            sb.Color3 = espColor
            sb.LineThickness = 0.03
            sb.SurfaceTransparency = 1
            sb.Parent = char
            data.SelectionBox = sb
            
        elseif espStyle == "Skeleton" then
            data.SkeletonLines = {}
        end
        
        activeESPs[p] = data
    else
        local data = activeESPs[p]
        if data.Billboard then
            local nl = data.Billboard:FindFirstChild("NL")
            local hl = data.Billboard:FindFirstChild("HL")
            if nl then nl.Visible = showNames; nl.TextColor3 = espColor end
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hl and hum then 
                hl.Visible = showHealth
                hl.TextColor3 = espColor 
                hl.Text = math.floor(hum.Health).."/"..math.floor(hum.MaxHealth)
            end
        end
        if data.Highlight then
            data.Highlight.FillColor = espColor
            data.Highlight.OutlineColor = espColor
        end
        if data.SelectionBox then
            data.SelectionBox.Color3 = espColor
        end
    end
end

Players.PlayerRemoving:Connect(function(p)
    cleanupESP(p)
    if playerVisuals[p] then
        if playerVisuals[p].tracer and playerVisuals[p].tracer.Remove then
            playerVisuals[p].tracer:Remove()
        end
        playerVisuals[p] = nil
    end
    if refreshPlayerDropdown then refreshPlayerDropdown() end
end)

Players.PlayerAdded:Connect(function(p) 
    p.CharacterAdded:Connect(function() cleanupESP(p) end)
    if refreshPlayerDropdown then refreshPlayerDropdown() end
end)

for _, p in ipairs(Players:GetPlayers()) do 
    if p ~= player then 
        p.CharacterAdded:Connect(function() cleanupESP(p) end) 
    end 
end

-- ============================================================
-- MAIN RENDER LOOP
-- ============================================================
local R15_CONNECTIONS = {
    { "Head", "UpperTorso" }, { "UpperTorso", "LowerTorso" },
    { "LowerTorso", "LeftUpperLeg" }, { "LeftUpperLeg", "LeftLowerLeg" },
    { "LowerTorso", "RightUpperLeg" }, { "RightUpperLeg", "RightLowerLeg" },
    { "UpperTorso", "LeftUpperArm" }, { "LeftUpperArm", "LeftLowerArm" }, { "LeftLowerArm", "LeftHand" },
    { "UpperTorso", "RightUpperArm" }, { "RightUpperArm", "RightLowerArm" }, { "RightLowerArm", "RightHand" },
}
local R6_CONNECTIONS = {
    { "Head", "Torso" }, { "Torso", "LeftArm" }, { "Torso", "RightArm" },
    { "Torso", "LeftLeg" }, { "Torso", "RightLeg" },
}

RunService.RenderStepped:Connect(function()
    camera = Workspace.CurrentCamera
    if not camera then return end
    local mousePos = UserInputService:GetMouseLocation()
    
    fovCircle.Position = mousePos
    fovCircle.Color = fovColor
    
    updateCrosshair(mousePos)
    
    -- ============================================================
    -- AIMLOCK LOGIC (HOLD / TOGGLE)
    -- ============================================================
    local isAimActive = false
    if aimEnabled then
        if aimMode == "Hold" then
            isAimActive = isAimKeyHeld()
        else
            isAimActive = aimToggledOn
        end
    end

    if isAimActive then
        if lockedTarget and lockedTarget.Character then
            local hum = lockedTarget.Character:FindFirstChildOfClass("Humanoid")
            if hum and hum.Health > 0 then
                local targetPart = lockedTarget.Character:FindFirstChild("Head") or lockedTarget.Character:FindFirstChild("HumanoidRootPart")
                if targetPart then
                    local valid = true
                    if checkTeam and player.Team and lockedTarget.Team and player.Team == lockedTarget.Team then valid = false end
                    if checkWalls and valid then
                        local origin = camera.CFrame.Position
                        local direction = (targetPart.Position - origin).Unit
                        local distance = (targetPart.Position - origin).Magnitude
                        local params = RaycastParams.new()
                        params.FilterDescendantsInstances = {player.Character, lockedTarget.Character}
                        params.FilterType = Enum.RaycastFilterType.Exclude
                        local rayResult = Workspace:Raycast(origin, direction * distance, params)
                        if rayResult and not rayResult.Instance:IsDescendantOf(lockedTarget.Character) then valid = false end
                    end
                    if valid then
                        local targetPos = targetPart.Position
                        if smoothnessVal <= 1 then 
                            camera.CFrame = CFrame.new(camera.CFrame.Position, targetPos)
                        else 
                            camera.CFrame = camera.CFrame:Lerp(CFrame.new(camera.CFrame.Position, targetPos), (21 - smoothnessVal) / 100) 
                        end
                    else
                        lockedTarget = nil
                    end
                end
            else
                lockedTarget = nil
            end
        end
        
        if not lockedTarget then
            local closestTarget, shortestDistance = nil, fovRadius
            for _, p in ipairs(Players:GetPlayers()) do
                if p == player then continue end
                if #targetedPlayers > 0 then
                    local isTargeted = false
                    for _, tName in ipairs(targetedPlayers) do if tName == p.Name then isTargeted = true break end end
                    if not isTargeted then continue end
                end
                if checkTeam and p.Team and player.Team and p.Team == player.Team then continue end
                local char = p.Character
                if not char then continue end
                local hum = char:FindFirstChildOfClass("Humanoid")
                if not hum or hum.Health <= 0 then continue end
                local targetPart = char:FindFirstChild("Head") or char:FindFirstChild("HumanoidRootPart")
                if not targetPart then continue end
                if checkWalls then
                    local origin = camera.CFrame.Position
                    local direction = (targetPart.Position - origin).Unit
                    local distance = (targetPart.Position - origin).Magnitude
                    local params = RaycastParams.new()
                    params.FilterDescendantsInstances = {player.Character, char}
                    params.FilterType = Enum.RaycastFilterType.Exclude
                    local rayResult = Workspace:Raycast(origin, direction * distance, params)
                    if rayResult and not rayResult.Instance:IsDescendantOf(char) then continue end
                end
                local screenPos, onScreen = camera:WorldToViewportPoint(targetPart.Position)
                if onScreen then
                    local dist = (Vector2.new(screenPos.X, screenPos.Y) - mousePos).Magnitude
                    if dist < shortestDistance then 
                        shortestDistance = dist
                        closestTarget = p 
                    end
                end
            end
            if closestTarget then
                lockedTarget = closestTarget
                local tp = lockedTarget.Character:FindFirstChild("Head") or lockedTarget.Character:FindFirstChild("HumanoidRootPart")
                if tp then
                    local targetPos = tp.Position
                    if smoothnessVal <= 1 then 
                        camera.CFrame = CFrame.new(camera.CFrame.Position, targetPos)
                    else 
                        camera.CFrame = camera.CFrame:Lerp(CFrame.new(camera.CFrame.Position, targetPos), (21 - smoothnessVal) / 100) 
                    end
                end
            end
        end
    else
        lockedTarget = nil
    end
    
    -- ============================================================
    -- ESP LOGIC
    -- ============================================================
    if espEnabled then
        for _, p in ipairs(Players:GetPlayers()) do
            ensureESP(p)
            local data = activeESPs[p]
            if not data or not p.Character then continue end
            
            local char = p.Character
            local hum = char:FindFirstChildOfClass("Humanoid")
            if data.Billboard and hum then
                local hl = data.Billboard:FindFirstChild("HL")
                if hl then hl.Text = math.floor(hum.Health).."/"..math.floor(hum.MaxHealth) end
            end
            
            if espStyle == "Skeleton" and data.SkeletonLines then
                if not char:FindFirstChild("HumanoidRootPart") then
                    for _, lineData in pairs(data.SkeletonLines) do 
                        lineData.drawing.Visible = false 
                    end
                    continue
                end
                
                local humanoid = char:FindFirstChildOfClass("Humanoid")
                local joints = {}
                local connections = R6_CONNECTIONS
                
                if humanoid and humanoid.RigType == Enum.HumanoidRigType.R15 then
                    joints = {
                        ["Head"] = char:FindFirstChild("Head"), ["UpperTorso"] = char:FindFirstChild("UpperTorso"),
                        ["LowerTorso"] = char:FindFirstChild("LowerTorso"), ["LeftUpperArm"] = char:FindFirstChild("LeftUpperArm"),
                        ["LeftLowerArm"] = char:FindFirstChild("LeftLowerArm"), ["LeftHand"] = char:FindFirstChild("LeftHand"),
                        ["RightUpperArm"] = char:FindFirstChild("RightUpperArm"), ["RightLowerArm"] = char:FindFirstChild("RightLowerArm"),
                        ["RightHand"] = char:FindFirstChild("RightHand"), ["LeftUpperLeg"] = char:FindFirstChild("LeftUpperLeg"),
                        ["LeftLowerLeg"] = char:FindFirstChild("LeftLowerLeg"), ["RightUpperLeg"] = char:FindFirstChild("RightUpperLeg"),
                        ["RightLowerLeg"] = char:FindFirstChild("RightLowerLeg"),
                    }
                    connections = R15_CONNECTIONS
                elseif humanoid and humanoid.RigType == Enum.HumanoidRigType.R6 then
                    joints = {
                        ["Head"] = char:FindFirstChild("Head"), ["Torso"] = char:FindFirstChild("Torso"),
                        ["LeftLeg"] = char:FindFirstChild("Left Leg"), ["RightLeg"] = char:FindFirstChild("Right Leg"),
                        ["LeftArm"] = char:FindFirstChild("Left Arm"), ["RightArm"] = char:FindFirstChild("Right Arm"),
                    }
                end
                
                for index, connection in ipairs(connections) do
                    local jointA = joints[connection[1]]
                    local jointB = joints[connection[2]]
                    
                    if jointA and jointB then
                        local posA, onScreenA = camera:WorldToViewportPoint(jointA.Position)
                        local posB, onScreenB = camera:WorldToViewportPoint(jointB.Position)
                        
                        local targetFrom = Vector2.new(posA.X, posA.Y)
                        local targetTo = Vector2.new(posB.X, posB.Y)
                        
                        local lineData = data.SkeletonLines[index]
                        if not lineData then
                            lineData = { drawing = Drawing.new("Line") }
                            data.SkeletonLines[index] = lineData
                        end
                        
                        lineData.drawing.From = targetFrom
                        lineData.drawing.To = targetTo
                        lineData.drawing.Color = espColor
                        lineData.drawing.Thickness = 1.5
                        lineData.drawing.Transparency = 1
                        
                        if onScreenA and onScreenB then
                            lineData.drawing.Visible = true
                        else
                            lineData.drawing.Visible = false
                        end
                    elseif data.SkeletonLines[index] then
                        data.SkeletonLines[index].drawing.Visible = false
                    end
                end
            end
        end
    else
        for p, _ in pairs(activeESPs) do cleanupESP(p) end
    end
    
    -- Tracers Logic
    if espEnabled and tracersEnabled then
        for _, p in ipairs(Players:GetPlayers()) do
            if p == player then continue end
            
            local vis = playerVisuals[p]
            if not vis then
                vis = {tracer = Drawing.new("Line")}
                playerVisuals[p] = vis
            end
            
            local char = p.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            local root = char and char:FindFirstChild("HumanoidRootPart")
            
            local valid = char and hum and root and hum.Health > 0
            local teamValid = not checkTeam or not player.Team or not p.Team or player.Team ~= p.Team
            
            if not valid or not teamValid then
                if vis.tracer then vis.tracer.Visible = false end
                continue
            end
            
            local screenPos, onScreen = camera:WorldToViewportPoint(root.Position)
            local screenVec = Vector2.new(screenPos.X, screenPos.Y)
            
            if onScreen then
                vis.tracer.Visible = true
                vis.tracer.From = getTracerOrigin()
                vis.tracer.To = screenVec
                vis.tracer.Color = espColor
                vis.tracer.Thickness = 1.5
            else
                vis.tracer.Visible = false
            end
        end
    else
        for _, vis in pairs(playerVisuals) do
            if vis.tracer then vis.tracer.Visible = false end
        end
    end
end)

-- ============================================================
-- MOVEMENT & ANTI-FLING (VRAIMENT CORRIGÉ & SANS LAG)
-- ============================================================
local function applyMovement()
    local char = player.Character
    if not char then return end
    local hum = char:FindFirstChildOfClass("Humanoid")
    if hum and not flyEnabled then hum.WalkSpeed = speedEnabled and walkSpeedVal or 16 end
end

function updateFly()
    local char = player.Character
    if not char then return end
    local root = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChildOfClass("Humanoid")
    if not root or not hum then return end
    if flyEnabled then
        hum.PlatformStand = true
        if not flyConn then
            flyConn = RunService.RenderStepped:Connect(function()
                if not flyEnabled or not player.Character then updateFly(); return end
                local currentRoot = player.Character:FindFirstChild("HumanoidRootPart")
                if not currentRoot then return end
                local cam = Workspace.CurrentCamera
                local moveDir = Vector3.new(0, 0, 0)
                if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + cam.CFrame.LookVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir - cam.CFrame.LookVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir - cam.CFrame.RightVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveDir = moveDir + cam.CFrame.RightVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.Space) then moveDir = moveDir + Vector3.new(0, 1, 0) end
                if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) or UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then moveDir = moveDir - Vector3.new(0, 1, 0) end
                if moveDir.Magnitude > 0 then moveDir = moveDir.Unit end
                currentRoot.AssemblyLinearVelocity = moveDir * flySpeedVal
                currentRoot.RotVelocity = Vector3.new(0, 0, 0)
            end)
        end
    else
        if flyConn then flyConn:Disconnect(); flyConn = nil end
        hum.PlatformStand = false
        applyMovement()
    end
end

player.CharacterAdded:Connect(function() 
    applyMovement() 
    updateFly()
    if currentAnimBundle then SetAnimationBundle(currentAnimBundle) end
end)

UserInputService.JumpRequest:Connect(function()
    if infJumpEnabled then
        local hum = player.Character and player.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum:ChangeState(Enum.HumanoidStateType.Jumping) end
    end
end)

-- ANTI-FLING OPTIMISÉ (Zéro Lag, Zéro Bug)
local antiFlingLastCheck = 0
local lastSafeCFrame = nil
local noclipLastCheck = 0

RunService.Stepped:Connect(function()
    local currentTime = tick()
    if antiFlingEnabled and player.Character and currentTime - antiFlingLastCheck > 0.05 then
        antiFlingLastCheck = currentTime
        
        local root = player.Character:FindFirstChild("HumanoidRootPart")
        local hum = player.Character:FindFirstChildOfClass("Humanoid")
        
        if root and hum and hum.Health > 0 and not flyEnabled then
            -- Mémorise la position au sol
            if hum.FloorMaterial ~= Enum.Material.Air then
                lastSafeCFrame = root.CFrame
            end

            local vel = root.AssemblyLinearVelocity
            -- Détection de fling (vélocité anormalement élevée)
            if vel.Magnitude > 150 then
                root.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                root.Velocity = Vector3.new(0, 0, 0)
                
                -- Si on est propulsé trop haut, on se replace au sol
                if lastSafeCFrame and root.Position.Y > lastSafeCFrame.Position.Y + 20 then
                    root.CFrame = lastSafeCFrame
                end
            end
        end
    end
    
    if noclipEnabled and player.Character and currentTime - (noclipLastCheck or 0) > 0.1 then
        noclipLastCheck = currentTime
        for _, part in ipairs(player.Character:GetDescendants()) do
            if part:IsA("BasePart") and part.CanCollide then part.CanCollide = false end
        end
    end
end)

RunService.Heartbeat:Connect(function()
    if antiAFK then
        pcall(function()
            local vu = game:GetService("VirtualUser")
            vu:CaptureController()
            vu:ClickButton2(Vector2.new())
        end)
    end
end)

local function updateLighting()
    if fullbright then
        Lighting.Brightness = 2
        Lighting.ClockTime = 14
        Lighting.FogEnd = 100000
        Lighting.GlobalShadows = false
        Lighting.OutdoorAmbient = Color3.fromRGB(128, 128, 128)
    else
        Lighting.Brightness = defaultLighting.Brightness
        Lighting.ClockTime = defaultLighting.ClockTime
        Lighting.FogEnd = defaultLighting.FogEnd
        Lighting.GlobalShadows = defaultLighting.GlobalShadows
        Lighting.OutdoorAmbient = defaultLighting.OutdoorAmbient
    end
end

-- ============================================================
-- INITIALISATION
-- ============================================================
applyMovement()
updateFly()

-- ============================================================
-- UI CONSTRUCTION (WindUI)
-- ============================================================
local Window = WindUI:CreateWindow({
    Title = "AnonHub",
    Author = "by Broly",
    Folder = "AnonHub",
    Icon = "solar:masks-outline",
    ToggleKey = Enum.KeyCode.K,
    Size = UDim2.fromOffset(1000, 520),
    MinSize = Vector2.new(1000, 520),
    MaxSize = Vector2.new(1000, 520),
    Topbar = { Height = 52, ButtonsType = "Default" },
})

if not Window then
    warn("[ AnonHub ] WindUI window already exists - UI creation skipped.")
    return
end

-- */  Home Tab  /*
do
    homeTab = Window:Tab({
        Title = "Home",
        Desc = "Informations & Discord",
        Icon = "solar:home-2-bold",
    })

    local homeSec = homeTab:Section({ Title = "Informations", Opened = true })

    homeSec:Paragraph({
        Title = "Created By",
        Desc = "Broly",
    })
    homeTab:Space()

    homeSec:Paragraph({
        Title = "User Roblox",
        Desc = "psych0pat3",
    })
    homeTab:Space()

    homeSec:Paragraph({
        Title = "User Discord",
        Desc = "crymyslf",
    })
    homeTab:Space()

    homeSec:Paragraph({
        Title = "Discord",
        Desc = "discord.gg/425usWgXr",
    })
    homeTab:Space()

    homeSec:Button({
        Title = "Join Discord",
        Icon = "solar:magic-stick-3-bold",
        Justify = "Center",
        Callback = function()
            if setclipboard then
                setclipboard("https://discord.gg/425usWgXr")
                WindUI:Notify({
                    Title = "Discord",
                    Content = "Invite link copied!",
                    Icon = "solar:check-square-bold",
                    Duration = 2,
                })
            end
        end,
    })
end

-- */  AimLock Tab  /*
do
    local combatTab = Window:Tab({
        Title = "AimLock",
        Desc = "Aimlock & aimbot settings",
        Icon = "solar:target-bold",
    })
    local aimSec = combatTab:Section({ Title = "Aimbot Settings", Opened = true })

    local aimToggle = aimSec:Toggle({
        Title = "Enable Aimbot",
        Desc = "Hold or toggle your aim key",
        Value = false,
        Flag = "AimEnabled",
        Callback = function(value)
            aimEnabled = value
            if not value then lockedTarget = nil; aimToggledOn = false end
        end,
    })
    combatTab:Space()

    aimSec:Dropdown({
        Title = "Aim Mode",
        Desc = "Hold: aim while the key is held | Toggle: press the key to switch",
        Values = { "Hold", "Toggle" },
        Value = "Hold",
        Flag = "AimMode",
        Callback = function(value)
            aimMode = value
            aimToggledOn = false
        end,
    })
    combatTab:Space()

    aimKeybind = aimSec:Keybind({
        Title = "Aim Key",
        Desc = "Key used to activate the aimbot",
        Value = "MouseRightButton",
        Flag = "AimKey",
    })
    combatTab:Space()

    aimSec:Slider({
        Title = "FOV Radius",
        Desc = "Max distance from your cursor to lock onto a target",
        Step = 1,
        Value = { Min = 10, Max = 1000, Default = 150 },
        Flag = "FovRadius",
        Callback = function(value)
            fovRadius = value
            fovCircle.Radius = value
        end,
    })
    combatTab:Space()

    aimSec:Slider({
        Title = "Smoothness (1=Instant)",
        Step = 1,
        Value = { Min = 1, Max = 20, Default = 10 },
        Flag = "Smoothness",
        Callback = function(value) smoothnessVal = value end,
    })
    combatTab:Space()

    aimSec:Toggle({
        Title = "Show FOV Circle",
        Desc = "Draw the aimlock FOV around your cursor",
        Value = false,
        Flag = "ShowFov",
        Callback = function(value) showFov = value; fovCircle.Visible = value end,
    })
    combatTab:Space()

    aimSec:Dropdown({
        Title = "FOV Color",
        Values = { "Red", "Cyan", "Blue", "Green", "Yellow", "Purple", "White" },
        Value = "Red",
        Flag = "FovColor",
        Callback = function(value)
            local c = {Red=Color3.fromRGB(255,50,50), Cyan=Color3.fromRGB(0,200,255), Blue=Color3.fromRGB(50,150,255), Green=Color3.fromRGB(50,255,100), Yellow=Color3.fromRGB(255,220,50), Purple=Color3.fromRGB(180,50,255), White=Color3.fromRGB(255,255,255)}
            fovColor = c[value] or Color3.fromRGB(255,50,50)
            fovCircle.Color = fovColor
        end,
    })
    combatTab:Space()

    aimSec:Toggle({
        Title = "Wall Check",
        Desc = "Ignore targets behind walls",
        Value = true,
        Flag = "WallCheck",
        Callback = function(value) checkWalls = value end,
    })
    combatTab:Space()

    aimSec:Toggle({
        Title = "Team Check",
        Desc = "Ignore players on your team",
        Value = false,
        Flag = "TeamCheck",
        Callback = function(value) checkTeam = value end,
    })
end

-- */  Visuals Tab  /*
do
    local visualsTab = Window:Tab({
        Title = "Visuals",
        Desc = "ESP, tracers and crosshair",
        Icon = "solar:eye-bold",
    })
    local espSec = visualsTab:Section({ Title = "ESP System", Opened = true })

    espSec:Toggle({
        Title = "Enable ESP",
        Value = false,
        Flag = "EspEnabled",
        Callback = function(value) espEnabled = value end,
    })
    visualsTab:Space()

    espSec:Toggle({
        Title = "Show Names",
        Value = true,
        Flag = "EspNames",
        Callback = function(value) showNames = value end,
    })
    visualsTab:Space()

    espSec:Toggle({
        Title = "Show Health",
        Value = true,
        Flag = "EspHealth",
        Callback = function(value) showHealth = value end,
    })
    visualsTab:Space()

    espSec:Dropdown({
        Title = "ESP Style",
        Values = { "Highlight", "3D Box", "Skeleton" },
        Value = "Highlight",
        Flag = "EspStyle",
        Callback = function(value)
            espStyle = value
            for _, p in ipairs(Players:GetPlayers()) do
                if p ~= player then cleanupESP(p) end
            end
        end,
    })
    visualsTab:Space()

    espSec:Dropdown({
        Title = "ESP Color",
        Values = { "Cyan", "Red", "Blue", "Green", "Yellow", "Purple", "White" },
        Value = "Cyan",
        Flag = "EspColor",
        Callback = function(value)
            local c = {Cyan=Color3.fromRGB(0,200,255), Red=Color3.fromRGB(255,50,50), Blue=Color3.fromRGB(50,150,255), Green=Color3.fromRGB(50,255,100), Yellow=Color3.fromRGB(255,220,50), Purple=Color3.fromRGB(180,50,255), White=Color3.fromRGB(255,255,255)}
            espColor = c[value] or Color3.fromRGB(0,200,255)
        end,
    })

    local tracSec = visualsTab:Section({ Title = "Tracers", Opened = true })

    tracSec:Toggle({
        Title = "Enable Tracers (Requires ESP)",
        Value = false,
        Flag = "TracersEnabled",
        Callback = function(value) tracersEnabled = value end,
    })
    visualsTab:Space()

    tracSec:Dropdown({
        Title = "Tracer Origin",
        Values = { "Bottom", "Center", "Top", "Mouse" },
        Value = "Bottom",
        Flag = "TracerOrigin",
        Callback = function(value) tracerOrigin = value end,
    })

    local crossSec = visualsTab:Section({ Title = "Custom Crosshair", Opened = true })

    crossSec:Toggle({
        Title = "Enable Crosshair",
        Value = false,
        Flag = "CrosshairEnabled",
        Callback = function(value) crosshairEnabled = value end,
    })
    visualsTab:Space()

    crossSec:Dropdown({
        Title = "Crosshair Style",
        Values = { "Cross", "Circle", "Dot" },
        Value = "Cross",
        Flag = "CrosshairStyle",
        Callback = function(value) crosshairStyle = value end,
    })
    visualsTab:Space()

    crossSec:Slider({
        Title = "Crosshair Gap",
        Step = 1,
        Value = { Min = 0, Max = 20, Default = 5 },
        Flag = "CrosshairGap",
        Callback = function(value) crosshairGap = value end,
    })
end

-- */  Players Tab  /*
do
    local playersTab = Window:Tab({
        Title = "Players",
        Desc = "Player list and actions",
        Icon = "solar:user-bold",
    })
    local playersSec = playersTab:Section({ Title = "Server Players", Opened = true })

    local selectedPlayerName = nil

    local playerDropdown = playersSec:Dropdown({
        Title = "Select Player",
        Desc = "Choose a player to act on",
        Values = {},
        Value = nil,
        AllowNone = true,
        Callback = function(value)
            selectedPlayerName = value
        end,
    })
    playersTab:Space()

    refreshPlayerDropdown = function()
        local names = {}
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= player then table.insert(names, p.Name) end
        end
        playerDropdown:Refresh(names)
    end
    refreshPlayerDropdown()
    playersTab:Space()

    playersSec:Button({
        Title = "Refresh Player List",
        Icon = "solar:refresh-circle-bold",
        Justify = "Center",
        Callback = function()
            refreshPlayerDropdown()
            WindUI:Notify({
                Title = "Player List",
                Content = "Refreshed!",
                Icon = "solar:check-square-bold",
                Duration = 1.5,
            })
        end,
    })
    playersTab:Space()

    playersSec:Button({
        Title = "Target Selected Player",
        Icon = "solar:target-bold",
        Justify = "Center",
        Callback = function()
            if not selectedPlayerName then
                WindUI:Notify({
                    Title = "No Player",
                    Content = "Select a player first.",
                    Icon = "solar:info-square-bold",
                    Duration = 2,
                })
                return
            end
            local found = false
            for i, name in ipairs(targetedPlayers) do
                if name == selectedPlayerName then
                    table.remove(targetedPlayers, i)
                    found = true
                    break
                end
            end
            if not found then table.insert(targetedPlayers, selectedPlayerName) end
        end,
    })
    playersTab:Space()

    playersSec:Button({
        Title = "TP to Selected Player",
        Icon = "solar:map-point-bold",
        Justify = "Center",
        Callback = function()
            local target = Players:FindFirstChild(selectedPlayerName or "")
            if not target then
                WindUI:Notify({
                    Title = "No Player",
                    Content = "Select a player first.",
                    Icon = "solar:info-square-bold",
                    Duration = 2,
                })
                return
            end
            local myChar = player.Character
            local tChar = target.Character
            if myChar and tChar and myChar:FindFirstChild("HumanoidRootPart") and tChar:FindFirstChild("HumanoidRootPart") then
                myChar.HumanoidRootPart.CFrame = tChar.HumanoidRootPart.CFrame + Vector3.new(0, 3, 0)
            end
        end,
    })
    playersTab:Space()

    playersSec:Button({
        Title = "Fling Selected Player",
        Icon = "solar:wind-bold",
        Justify = "Center",
        Callback = function()
            local target = Players:FindFirstChild(selectedPlayerName or "")
            if not target then
                WindUI:Notify({
                    Title = "No Player",
                    Content = "Select a player first.",
                    Icon = "solar:info-square-bold",
                    Duration = 2,
                })
                return
            end
            SkidFling(target)
        end,
    })
    playersTab:Space()

    playersSec:Button({
        Title = "Clear Target List",
        Icon = "solar:trash-bin-trash-bold",
        Justify = "Center",
        Callback = function() targetedPlayers = {} end,
    })
end

-- */  Movement Tab  /*
do
    local movementTab = Window:Tab({
        Title = "Movement",
        Desc = "Speed, fly, noclip and more",
        Icon = "solar:running-bold",
    })
    local moveSec = movementTab:Section({ Title = "Physics", Opened = true })

    speedToggle = moveSec:Toggle({
        Title = "Custom Speed",
        Value = false,
        Flag = "SpeedEnabled",
        Callback = function(value) speedEnabled = value; applyMovement() end,
    })
    movementTab:Space()

    moveSec:Slider({
        Title = "Speed Value",
        Step = 1,
        Value = { Min = 16, Max = 200, Default = 50 },
        Flag = "SpeedValue",
        Callback = function(value) walkSpeedVal = value; if speedEnabled then applyMovement() end end,
    })
    movementTab:Space()

    speedKeybind = moveSec:Keybind({
        Title = "Speed Toggle Key",
        Desc = "Press to toggle custom speed",
        Value = "None",
    })
    movementTab:Space()

    flyToggle = moveSec:Toggle({
        Title = "Super Fast Fly",
        Value = false,
        Flag = "FlyEnabled",
        Callback = function(value) flyEnabled = value; updateFly() end,
    })
    movementTab:Space()

    moveSec:Slider({
        Title = "Fly Speed (Max 1000)",
        Step = 1,
        Value = { Min = 50, Max = 1000, Default = 300 },
        Flag = "FlySpeed",
        Callback = function(value) flySpeedVal = value end,
    })
    movementTab:Space()

    flyKeybind = moveSec:Keybind({
        Title = "Fly Toggle Key",
        Desc = "Press to toggle fly",
        Value = "None",
    })
    movementTab:Space()

    moveSec:Toggle({
        Title = "Anti-Fling",
        Desc = "Prevents being flung by other players",
        Value = false,
        Flag = "AntiFling",
        Callback = function(value) antiFlingEnabled = value end,
    })
    movementTab:Space()

    infJumpToggle = moveSec:Toggle({
        Title = "Infinite Jump",
        Value = false,
        Flag = "InfJump",
        Callback = function(value) infJumpEnabled = value end,
    })
    movementTab:Space()

    jumpKeybind = moveSec:Keybind({
        Title = "Jump Toggle Key",
        Desc = "Press to toggle infinite jump",
        Value = "None",
    })
    movementTab:Space()

    noclipToggle = moveSec:Toggle({
        Title = "Noclip",
        Value = false,
        Flag = "Noclip",
        Callback = function(value) noclipEnabled = value end,
    })
    movementTab:Space()

    noclipKeybind = moveSec:Keybind({
        Title = "Noclip Toggle Key",
        Desc = "Press to toggle noclip",
        Value = "None",
    })
    movementTab:Space()

    moveSec:Button({
        Title = "Reset Character",
        Icon = "solar:restart-bold",
        Justify = "Center",
        Callback = function()
            local hum = player.Character and player.Character:FindFirstChildOfClass("Humanoid")
            if hum then hum.Health = 0 end
        end,
    })
end

-- */  Bundles Tab  /*
do
    local animTab = Window:Tab({
        Title = "Bundles",
        Desc = "Custom animation bundles",
        Icon = "solar:dumbbell-bold",
    })
    local animSec = animTab:Section({ Title = "Movement Bundles", Opened = true })

    local animDropdown = animSec:Dropdown({
        Title = "Animation Bundle",
        Desc = "Select a bundle to apply to your character",
        Values = { "Default", "Ninja", "Cartoony", "Stylish", "Zombie", "Mocap" },
        Value = "Default",
        Flag = "AnimBundle",
        Callback = function(value)
            SetAnimationBundle(value)
        end,
    })
    animTab:Space()

    animSec:Button({
        Title = "Reset to Default Animations",
        Icon = "solar:restart-bold",
        Justify = "Center",
        Callback = function()
            SetAnimationBundle("Default")
            if animDropdown and animDropdown.Select then animDropdown:Select("Default") end
        end,
    })
end

-- */  Config Tab  /*
do
    local configTab = Window:Tab({
        Title = "Config",
        Desc = "Save and load configurations",
        Icon = "solar:folder-with-files-bold",
    })
    local configSec = configTab:Section({ Title = "Configuration Manager", Opened = true })

    local configName = "default"
    local ConfigManager = Window.ConfigManager

    local configNameInput = configSec:Input({
        Title = "Config Name",
        Value = "default",
        Callback = function(value)
            if value and value ~= "" then configName = value end
        end,
    })
    configTab:Space()

    local configDropdown = configSec:Dropdown({
        Title = "All Configs",
        Desc = "Select an existing config to edit",
        Values = (ConfigManager and ConfigManager:AllConfigs()) or {},
        Value = nil,
        AllowNone = true,
        Callback = function(value)
            if value then
                configName = value
                configNameInput:Set(value)
            end
        end,
    })
    configTab:Space()

    configSec:Button({
        Title = "Save Current Config",
        Icon = "solar:check-square-bold",
        Justify = "Center",
        Callback = function()
            if not ConfigManager then
                WindUI:Notify({
                    Title = "Config",
                    Content = "Config system unavailable (Studio or no writefile).",
                    Icon = "solar:info-square-bold",
                    Duration = 3,
                })
                return
            end
            Window.CurrentConfig = ConfigManager:Config(configName)
            if Window.CurrentConfig:Save() then
                WindUI:Notify({
                    Title = "Config Saved",
                    Content = "Config '" .. configName .. "' saved",
                    Icon = "solar:check-square-bold",
                    Duration = 3,
                })
            end
            configDropdown:Refresh(ConfigManager:AllConfigs())
        end,
    })
    configTab:Space()

    configSec:Button({
        Title = "Load Config",
        Icon = "solar:refresh-circle-bold",
        Justify = "Center",
        Callback = function()
            if not ConfigManager then
                WindUI:Notify({
                    Title = "Config",
                    Content = "Config system unavailable (Studio or no writefile).",
                    Icon = "solar:info-square-bold",
                    Duration = 3,
                })
                return
            end
            Window.CurrentConfig = ConfigManager:CreateConfig(configName)
            local ok, err = Window.CurrentConfig:Load()
            if ok then
                WindUI:Notify({
                    Title = "Config Loaded",
                    Content = "Config '" .. configName .. "' loaded",
                    Icon = "solar:check-square-bold",
                    Duration = 3,
                })
            else
                WindUI:Notify({
                    Title = "Config",
                    Content = "Config '" .. configName .. "' not found",
                    Icon = "solar:info-square-bold",
                    Duration = 3,
                })
            end
            configDropdown:Refresh(ConfigManager:AllConfigs())
        end,
    })
    configTab:Space()

    configSec:Button({
        Title = "Delete Config",
        Icon = "solar:trash-bin-trash-bold",
        Justify = "Center",
        Callback = function()
            if not ConfigManager then
                WindUI:Notify({
                    Title = "Config",
                    Content = "Config system unavailable (Studio or no writefile).",
                    Icon = "solar:info-square-bold",
                    Duration = 3,
                })
                return
            end
            local cfg = ConfigManager:CreateConfig(configName)
            local ok = cfg:Delete()
            if ok then
                WindUI:Notify({
                    Title = "Config Deleted",
                    Content = "Config '" .. configName .. "' deleted",
                    Icon = "solar:check-square-bold",
                    Duration = 3,
                })
            end
            configDropdown:Refresh(ConfigManager:AllConfigs())
        end,
    })
end

-- */  Settings Tab  /*
do
    local settingsTab = Window:Tab({
        Title = "Settings",
        Desc = "Menu and misc settings",
        Icon = "solar:settings-bold",
    })
    local setSec = settingsTab:Section({ Title = "Menu Settings", Opened = true })

    setSec:Keybind({
        Title = "Menu Toggle Key",
        Desc = "Open / close the menu",
        Value = "K",
        Callback = function(v)
            Window:SetToggleKey(Enum.KeyCode[v])
        end,
    })
    settingsTab:Space()

    setSec:Toggle({
        Title = "Anti-AFK",
        Desc = "Prevents idle disconnection",
        Value = false,
        Flag = "AntiAFK",
        Callback = function(value) antiAFK = value end,
    })
    settingsTab:Space()

    setSec:Toggle({
        Title = "Fullbright",
        Value = false,
        Flag = "Fullbright",
        Callback = function(value) fullbright = value; updateLighting() end,
    })
    settingsTab:Space()

    setSec:Button({
        Title = "Rejoin Server",
        Icon = "solar:restart-bold",
        Justify = "Center",
        Callback = function()
            TeleportService:Teleport(game.PlaceId, player)
        end,
    })
    settingsTab:Space()

    setSec:Button({
        Title = "Copy Username",
        Icon = "solar:user-bold",
        Justify = "Center",
        Callback = function()
            if setclipboard then setclipboard(player.Name) end
        end,
    })
    settingsTab:Space()

    setSec:Button({
        Title = "Join Discord",
        Icon = "solar:magic-stick-3-bold",
        Justify = "Center",
        Callback = function()
            if setclipboard then
                setclipboard("https://discord.gg/425usWgXr")
                WindUI:Notify({
                    Title = "Discord",
                    Content = "Invite link copied!",
                    Icon = "solar:check-square-bold",
                    Duration = 2,
                })
            end
        end,
    })
    settingsTab:Space()

    setSec:Button({
        Title = "Destroy Script",
        Icon = "solar:trash-bin-trash-bold",
        Justify = "Center",
        Callback = function()
            fullbright = false
            updateLighting()

            if fovCircle then fovCircle:Remove() end
            for _, l in pairs(crosshairDrawings) do
                if l.Remove then l:Remove() end
            end
            for _, p in ipairs(Players:GetPlayers()) do cleanupESP(p) end
            for _, vis in pairs(playerVisuals) do
                if vis.tracer and vis.tracer.Remove then vis.tracer:Remove() end
            end
            activeESPs = {}
            playerVisuals = {}
            if flyConn then flyConn:Disconnect() end
            Window:Destroy()
        end,
    })
end

-- Home tab ouvert par défaut
if homeTab and homeTab.Select then
    homeTab:Select()
end

-- ============================================================
-- KEYBIND TOGGLE HANDLER
-- ============================================================
local function isKeybindPressed(keybind, input)
    if not keybind or not keybind.Value then return false end
    local key = keybind.Value
    if key == "None" or key == "" then return false end
    if input.UserInputType == Enum.UserInputType.Keyboard then
        return input.KeyCode.Name == key
    elseif input.UserInputType == Enum.UserInputType.MouseButton1 then
        return key == "MouseLeftButton" or key == "MouseLeft"
    elseif input.UserInputType == Enum.UserInputType.MouseButton2 then
        return key == "MouseRightButton" or key == "MouseRight"
    end
    return false
end

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end

    -- Aimlock Toggle Logic
    if aimEnabled and aimMode == "Toggle" then
        local aimKey = getAimKeyEnum()
        if aimKey then
            local isMatch = false
            if aimKey.EnumType == Enum.KeyCode and input.KeyCode == aimKey then isMatch = true end
            if aimKey.EnumType == Enum.UserInputType and input.UserInputType == aimKey then isMatch = true end
            if isMatch then
                aimToggledOn = not aimToggledOn
            end
        end
    end

    if isKeybindPressed(speedKeybind, input) then speedToggle:Set(not speedToggle.Value) end
    if isKeybindPressed(flyKeybind, input) then flyToggle:Set(not flyToggle.Value) end
    if isKeybindPressed(jumpKeybind, input) then infJumpToggle:Set(not infJumpToggle.Value) end
    if isKeybindPressed(noclipKeybind, input) then noclipToggle:Set(not noclipToggle.Value) end
end)

-- ============================================================
-- INITIALISATION FINALE
-- ============================================================
task.spawn(function()
    WindUI:Notify({
        Title = "AnonHub",
        Content = "Hello, " .. player.DisplayName .. "!",
        Icon = "solar:rocket-bold",
        Duration = 4,
        CanClose = true,
    })
end)

print("AnonHub Loaded!")


