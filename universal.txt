-- ============================================================
-- ⛓ FUCKER // crymyslf [ULTIMATE GLASS EDITION - FINAL OPTIMIZED]
-- ============================================================

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")
local TeleportService = game:GetService("TeleportService")
local Lighting = game:GetService("Lighting")
local ContextActionService = game:GetService("ContextActionService")
local HttpService = game:GetService("HttpService")
local TweenService = game:GetService("TweenService")

local player = Players.LocalPlayer
local UserId = player.UserId

-- ============================================================
-- GLOBAL VARIABLES
-- ============================================================
local aimEnabled = false
local aimKey = Enum.UserInputType.MouseButton2
local fovRadius = 150
local showFov = true
local smoothnessVal = 10
local checkWalls = true
local checkTeam = true
local lockedTarget = nil
local targetedPlayers = {}

local espEnabled = false
local showNames = true
local showHealth = true
local espColor = Color3.fromRGB(255, 50, 50)
local activeESPs = {}

local speedEnabled = false
local walkSpeedVal = 50
local flyEnabled = false
local flySpeedVal = 300
local noclipEnabled = false
local infJumpEnabled = false
local antiFlingEnabled = false

local antiAFK = false
local fullbright = false
local stealthMode = false

local menuToggleKey = Enum.KeyCode.K
local savedConfigs = {}
local flyConn = nil
local configStatusText = "Ready"

local flyKeybind = nil
local speedKeybind = nil
local jumpKeybind = nil
local noclipKeybind = nil

local currentAnimBundle = "Mocap"

-- UI Toggle Objects for perfect synchronization
local speedToggleObj = nil
local flyToggleObj = nil
local infJumpToggleObj = nil
local noclipToggleObj = nil

-- ============================================================
-- REAL ANIMATION SYSTEM (ALL LATEST BUNDLES)
-- ============================================================
local function ApplyAnimations(Animations, Character)
    local Animate = Character:FindFirstChild('Animate') or Character:WaitForChild('Animate', 5)
    if not Animate then return end
    
    for Animation, Ids in next, Animations do
        local AnimObject = Animate:FindFirstChild(Animation)
        if not AnimObject then continue end
        
        for i, id in next, Ids do
            local AnimationObject
            if Animation == 'idle' then
                AnimationObject = AnimObject:FindFirstChild('Animation'..tostring(i))
            elseif Animation == 'swim' and i == 2 then 
                local swimidle = Animate:FindFirstChild('swimidle')
                AnimationObject = swimidle and swimidle:FindFirstChild('SwimIdle')
            else
                local firststchar = string.upper(string.sub(Animation,1,1))
                local restchar = string.sub(Animation,2,#Animation)
                local fullstr = firststchar..restchar
                AnimationObject = AnimObject:FindFirstChild(fullstr..'Anim') or AnimObject:FindFirstChild(fullstr)
            end
            
            if AnimationObject and id then
                AnimationObject.AnimationId = "rbxassetid://"..tostring(id)
            end
        end
    end
end

local AnimationBundles = {
    ["Ninja"] = {idle = {656117400, 656118341}, walk = {656121766}, run = {656118852}, jump = {656117878}, fall = {656115606}, swim = {656119721, 656121397}, climb = {656114359}},
    ["Cartoony"] = {idle = {742637544, 742638445}, walk = {742640026}, run = {742638842}, jump = {742637942}, fall = {742637151}, swim = {742639220, 742639812}, climb = {742636889}},
    ["Stylish"] = {idle = {616136790, 616138447}, walk = {616146177}, run = {616140816}, jump = {616139451}, fall = {616134815}, swim = {616143378, 616144772}, climb = {616133594}},
    ["Toy"] = {idle = {782841498, 782845736}, walk = {782843345}, run = {782842708}, jump = {782847020}, fall = {782846423}, swim = {782844582, 782845186}, climb = {782843869}},
    ["Bubbly"] = {idle = {910004836, 910009958}, walk = {910034870}, run = {910025107}, jump = {910016857}, fall = {910001910}, swim = {910028158, 910030921}, climb = {909997997}},
    ["Levitation"] = {idle = {616006778, 616008087}, walk = {616013216}, run = {616010382}, jump = {616008936}, fall = {616005863}, swim = {616011509, 616012453}, climb = {616003713}},
    ["Robot"] = {idle = {616088211, 616089559}, walk = {616095330}, run = {616091570}, jump = {616090535}, fall = {616087089}, swim = {616092998, 616094091}, climb = {616086039}},
    ["Vampire"] = {idle = {1083445855, 1083450166}, walk = {1083473930}, run = {1083462077}, jump = {1083455352}, fall = {1083443587}, swim = {1083464683, 1083467779}, climb = {1083439238}},
    ["Zombie"] = {idle = {616158929, 616160636}, walk = {616168032}, run = {616163682}, jump = {616161997}, fall = {616157476}, swim = {616165109, 616166655}, climb = {616156119}},
    ["Elder"] = {idle = {845397899, 845400520}, walk = {845403856}, run = {845386501}, jump = {845398858}, fall = {845396048}, swim = {845401742, 845403127}, climb = {845392038}},
    ["Superhero"] = {idle = {616111295, 616113536}, walk = {616122287}, run = {616117076}, jump = {616115533}, fall = {616108001}, swim = {616119360, 616120861}, climb = {616104706}},
    ["Mage"] = {idle = {707742142, 707855907}, walk = {707897309}, run = {707861613}, jump = {707853694}, fall = {707829716}, swim = {707876443, 707894699}, climb = {707826056}},
    ["Werewolf"] = {idle = {1083195517, 1083214717}, walk = {1083178339}, run = {1083216690}, jump = {1083218792}, fall = {1083189019}, swim = {1083222527, 1083225406}, climb = {1083182000}},
    ["Astronaut"] = {idle = {891621366, 891633237}, walk = {891636393}, run = {891636393}, jump = {891627522}, fall = {891617961}, swim = {891639666, 891663592}, climb = {891609353}},
    ["Knight"] = {idle = {657595757, 657568135}, walk = {657552124}, run = {657564596}, jump = {658409194}, fall = {657600338}, swim = {657560551, 657557095}, climb = {658360781}},
    ["Pirate"] = {idle = {750781874, 750782770}, walk = {750785693}, run = {750783738}, jump = {750782230}, fall = {750780242}, swim = {750784579, 750785176}, climb = {750779899}},
    ["Rthro"] = {idle = {2510197257, 2510196951}, walk = {2510202577}, run = {2510198475}, jump = {2510197830}, fall = {2510195892}, swim = {2510199791, 2510201162}, climb = {2510192778}},
    ["Confident"] = {idle = {1069977950, 1069987858}, walk = {1070017263}, run = {1070001516}, jump = {1069984524}, fall = {1069973677}, swim = {1070009914, 1070012133}, climb = {1069946257}},
    ["Popstar"] = {idle = {1212900985, 1212954651}, walk = {1212980338}, run = {1212980348}, jump = {1212954642}, fall = {1212900995}, swim = {1212852603, 1212998578}, climb = {1213044953}},
    ["Patrol"] = {idle = {1149612882, 1150842221}, walk = {1151231493}, run = {1150967949}, jump = {1150944216}, fall = {1148863382}, swim = {1151204998, 1151221899}, climb = {1148811837}},
    ["Sneaky"] = {idle = {1132473842, 1132477671}, walk = {1132510133}, run = {1132494274}, jump = {1132489853}, fall = {1132469004}, swim = {1132500520, 1132506407}, climb = {1132461372}},
    ["Mocap"] = {idle = {507766388, 507766951}, walk = {507777826}, run = {507767714}, jump = {507765000}, fall = {507767968}, swim = {507784897, 507785072}, climb = {507765644}},
    ["Oldschool"] = {idle = {5319828216, 5319831086}, walk = {5319847204}, run = {5319844329}, jump = {5319841935}, fall = {5319839762}, swim = {5319850266, 5319852613}, climb = {5319816685}},
    ["Princess"] = {idle = {941003647, 941013098}, walk = {941028902}, run = {941015281}, jump = {941008832}, fall = {941000007}, swim = {941018893, 941025398}, climb = {940996062}},
    ["Cowboy"] = {idle = {1014390418, 1014398616}, walk = {1014421541}, run = {1014401683}, jump = {1014394726}, fall = {1014384571}, swim = {1014406523, 1014411816}, climb = {1014380606}}
}

local function SetAnimationBundle(bundleName)
    currentAnimBundle = bundleName
    local Character = player.Character
    if not Character then return end
    local bundle = AnimationBundles[bundleName]
    if not bundle then return end
    ApplyAnimations(bundle, Character)
end

-- ============================================================
-- ROBUST FLING SYSTEM (KILASIK's Method)
-- ============================================================
local function SkidFling(TargetPlayer)
    local Character = player.Character
    local Humanoid = Character and Character:FindFirstChildOfClass("Humanoid")
    local RootPart = Humanoid and Humanoid.RootPart
    local TCharacter = TargetPlayer.Character
    if not TCharacter or not Character or not RootPart then return end
    
    local THumanoid = TCharacter:FindFirstChildOfClass("Humanoid")
    local TRootPart = THumanoid and THumanoid.RootPart
    if not THumanoid or not TRootPart then return end
    if THumanoid.Sit then return end
    
    local OldPos = RootPart.CFrame
    local FPDH = Workspace.FallenPartsDestroyHeight
    Workspace.FallenPartsDestroyHeight = 0/0 
    
    local BV = Instance.new("BodyVelocity")
    BV.Parent = RootPart
    BV.Velocity = Vector3.new(0, 0, 0)
    BV.MaxForce = Vector3.new(9e9, 9e9, 9e9)
    
    Humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, false)
    Character.PrimaryPart = RootPart
    
    local flingActive = true
    task.delay(2, function() flingActive = false end)
    
    local Angle = 0
    while flingActive and TCharacter and TCharacter.Parent and TRootPart do
        Angle = Angle + 100
        local pos1 = CFrame.new(TRootPart.Position) * CFrame.new(0, 1.5, 0) * CFrame.Angles(math.rad(Angle), 0, 0)
        RootPart.CFrame = pos1
        Character:SetPrimaryPartCFrame(pos1)
        RootPart.Velocity = Vector3.new(9e7, 9e8, 9e7)
        RootPart.RotVelocity = Vector3.new(9e8, 9e8, 9e8)
        task.wait()
        
        local pos2 = CFrame.new(TRootPart.Position) * CFrame.new(0, -1.5, 0) * CFrame.Angles(math.rad(Angle), 0, 0)
        RootPart.CFrame = pos2
        Character:SetPrimaryPartCFrame(pos2)
        RootPart.Velocity = Vector3.new(9e7, 9e8, 9e7)
        RootPart.RotVelocity = Vector3.new(9e8, 9e8, 9e8)
        task.wait()
    end
    
    BV:Destroy()
    Humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
    Workspace.FallenPartsDestroyHeight = FPDH
    
    RootPart.CFrame = OldPos * CFrame.new(0, 0.5, 0)
    Character:SetPrimaryPartCFrame(OldPos * CFrame.new(0, 0.5, 0))
    Humanoid:ChangeState(Enum.HumanoidStateType.GettingUp)
    for _, part in pairs(Character:GetChildren()) do
        if part:IsA("BasePart") then
            part.Velocity, part.RotVelocity = Vector3.new(), Vector3.new()
        end
    end
end

-- ============================================================
-- FOV CIRCLE
-- ============================================================
local fovCircle = Drawing.new("Circle")
fovCircle.Visible = false
fovCircle.Thickness = 2
fovCircle.NumSides = 64
fovCircle.Filled = false
fovCircle.Transparency = 0.8
fovCircle.Color = Color3.fromRGB(255, 50, 50)
fovCircle.Radius = fovRadius

-- ============================================================
-- UNIQUE GLASS UI SYSTEM
-- ============================================================
local Theme = {
    BG = Color3.fromRGB(15, 15, 20),
    Glass = Color3.fromRGB(25, 25, 35),
    Surface = Color3.fromRGB(35, 35, 45),
    Accent = Color3.fromRGB(255, 50, 50),
    Text = Color3.fromRGB(245, 245, 250),
    TextDim = Color3.fromRGB(140, 140, 160)
}

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "FuckerGlassUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.DisplayOrder = 999999
if syn and syn.protect_gui then
    syn.protect_gui(ScreenGui)
    ScreenGui.Parent = game.CoreGui
elseif get_hidden_gui then
    ScreenGui.Parent = get_hidden_gui()
else
    ScreenGui.Parent = game.CoreGui
end

local MinimizeIcon = Instance.new("ImageButton")
MinimizeIcon.Size = UDim2.new(0, 50, 0, 50)
MinimizeIcon.Position = UDim2.new(0, 20, 0.5, -25)
MinimizeIcon.BackgroundTransparency = 0.1
MinimizeIcon.BackgroundColor3 = Theme.Glass
MinimizeIcon.Visible = false
MinimizeIcon.Parent = ScreenGui
Instance.new("UICorner", MinimizeIcon).CornerRadius = UDim.new(1, 0)
Instance.new("UIStroke", MinimizeIcon).Color = Theme.Accent
local iconText = Instance.new("TextLabel")
iconText.Size = UDim2.new(1, 0, 1, 0)
iconText.BackgroundTransparency = 1
iconText.Text = "⛓"
iconText.TextColor3 = Theme.Accent
iconText.TextSize = 24
iconText.Font = Enum.Font.GothamBold
iconText.Parent = MinimizeIcon

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 650, 0, 450)
MainFrame.Position = UDim2.new(0.5, -325, 0.5, -225)
MainFrame.BackgroundTransparency = 0.1
MainFrame.BackgroundColor3 = Theme.Glass
MainFrame.BorderSizePixel = 0
MainFrame.Parent = ScreenGui

local mainStroke = Instance.new("UIStroke")
mainStroke.Color = Theme.Accent
mainStroke.Transparency = 0.7
mainStroke.Thickness = 1.5
mainStroke.Parent = MainFrame

local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 16)
mainCorner.Parent = MainFrame

local Sidebar = Instance.new("Frame")
Sidebar.Size = UDim2.new(0, 180, 1, 0)
local sidebarCorner = Instance.new("UICorner")
sidebarCorner.CornerRadius = UDim.new(0, 16)
sidebarCorner.Parent = Sidebar
Sidebar.BackgroundTransparency = 0.2
Sidebar.BackgroundColor3 = Color3.fromRGB(20, 20, 28)
Sidebar.Parent = MainFrame

local ProfileFrame = Instance.new("Frame")
ProfileFrame.Size = UDim2.new(1, 0, 0, 110)
ProfileFrame.BackgroundTransparency = 1
ProfileFrame.Parent = Sidebar

local PDP = Instance.new("ImageLabel")
PDP.Size = UDim2.new(0, 60, 0, 60)
PDP.Position = UDim2.new(0.5, -30, 0, 10)
PDP.BackgroundTransparency = 0.2
PDP.BackgroundColor3 = Theme.Accent
PDP.Parent = ProfileFrame
Instance.new("UICorner", PDP).CornerRadius = UDim.new(1, 0)
Instance.new("UIStroke", PDP).Color = Theme.Accent

task.spawn(function()
    local success, thumb = pcall(function()
        return Players:GetUserThumbnailAsync(UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size150x150)
    end)
    if success then PDP.Image = thumb end
end)

local UserNameLabel = Instance.new("TextLabel")
UserNameLabel.Size = UDim2.new(1, 0, 0, 20)
UserNameLabel.Position = UDim2.new(0, 0, 0, 75)
UserNameLabel.BackgroundTransparency = 1
UserNameLabel.Text = player.DisplayName
UserNameLabel.TextColor3 = Theme.Text
UserNameLabel.TextSize = 16
UserNameLabel.Font = Enum.Font.GothamBold
UserNameLabel.Parent = ProfileFrame

local CreatorLabel = Instance.new("TextLabel")
CreatorLabel.Size = UDim2.new(1, 0, 0, 15)
CreatorLabel.Position = UDim2.new(0, 0, 0, 95)
CreatorLabel.BackgroundTransparency = 1
CreatorLabel.Text = "Universal by crymyslf"
CreatorLabel.TextColor3 = Theme.Accent
CreatorLabel.TextSize = 11
CreatorLabel.Font = Enum.Font.Gotham
CreatorLabel.Parent = ProfileFrame

local TabContainer = Instance.new("Frame")
TabContainer.Size = UDim2.new(1, 0, 1, -120)
TabContainer.Position = UDim2.new(0, 0, 0, 120)
TabContainer.BackgroundTransparency = 1
TabContainer.Parent = Sidebar

local TabLayout = Instance.new("UIListLayout")
TabLayout.Padding = UDim.new(0, 8)
TabLayout.SortOrder = Enum.SortOrder.LayoutOrder
TabLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
TabLayout.Parent = TabContainer

local ContentArea = Instance.new("Frame")
ContentArea.Size = UDim2.new(1, -180, 1, 0)
ContentArea.Position = UDim2.new(0, 180, 0, 0)
ContentArea.BackgroundTransparency = 1
ContentArea.Parent = MainFrame

local MinimizeBtn = Instance.new("TextButton")
MinimizeBtn.Size = UDim2.new(0, 30, 0, 30)
MinimizeBtn.Position = UDim2.new(1, -75, 0, 5)
MinimizeBtn.BackgroundTransparency = 0.2
MinimizeBtn.BackgroundColor3 = Theme.Surface
MinimizeBtn.Text = "−"
MinimizeBtn.TextColor3 = Theme.Text
MinimizeBtn.TextSize = 18
MinimizeBtn.Font = Enum.Font.GothamBold
MinimizeBtn.Parent = MainFrame
Instance.new("UICorner", MinimizeBtn).CornerRadius = UDim.new(0, 6)

local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 30, 0, 30)
CloseBtn.Position = UDim2.new(1, -40, 0, 5)
CloseBtn.BackgroundTransparency = 0.2
CloseBtn.BackgroundColor3 = Theme.Accent
CloseBtn.Text = "✕"
CloseBtn.TextColor3 = Theme.Text
CloseBtn.TextSize = 16
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Parent = MainFrame
Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(0, 6)

local dragRegion = Instance.new("Frame")
dragRegion.Size = UDim2.new(1, 0, 0, 30)
dragRegion.BackgroundTransparency = 1
dragRegion.Parent = MainFrame

local draggingUI = false
local dragStart, startPos

dragRegion.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        draggingUI = true
        dragStart = input.Position
        startPos = MainFrame.Position
    end
end)

dragRegion.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        draggingUI = false
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if draggingUI and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

local Tabs = {}
local CurrentTab = nil

local function CreateTab(name)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, -10, 0, 36)
    btn.BackgroundTransparency = 0.2
    btn.BackgroundColor3 = Theme.Surface
    btn.Text = name
    btn.TextColor3 = Theme.TextDim
    btn.TextSize = 13
    btn.Font = Enum.Font.GothamBold
    btn.Parent = TabContainer
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)
    
    btn.MouseEnter:Connect(function()
        if CurrentTab ~= name then
            TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundTransparency = 0.1}):Play()
        end
    end)
    btn.MouseLeave:Connect(function()
        if CurrentTab ~= name then
            TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundTransparency = 0.2}):Play()
        end
    end)
    
    local content = Instance.new("ScrollingFrame")
    content.Size = UDim2.new(1, -20, 1, -20)
    content.Position = UDim2.new(0, 10, 0, 10)
    content.BackgroundTransparency = 1
    content.BorderSizePixel = 0
    content.ScrollBarThickness = 4
    content.ScrollBarImageColor3 = Theme.Accent
    content.AutomaticCanvasSize = Enum.AutomaticSize.Y
    content.Visible = false
    content.Parent = ContentArea
    
    local layout = Instance.new("UIListLayout", content)
    layout.Padding = UDim.new(0, 8)
    layout.SortOrder = Enum.SortOrder.LayoutOrder
    
    Tabs[name] = {Btn = btn, Content = content}
    
    btn.MouseButton1Click:Connect(function()
        for n, t in pairs(Tabs) do
            t.Content.Visible = false
            t.Btn.BackgroundTransparency = 0.2
            t.Btn.BackgroundColor3 = Theme.Surface
            t.Btn.TextColor3 = Theme.TextDim
        end
        content.Visible = true
        btn.BackgroundTransparency = 0
        btn.BackgroundColor3 = Theme.Accent
        btn.TextColor3 = Theme.Text
        CurrentTab = name
    end)
    
    return content
end

local function CreateSection(parent, title)
    local sec = Instance.new("Frame")
    sec.Size = UDim2.new(1, 0, 0, 0)
    sec.BackgroundTransparency = 1
    sec.AutomaticSize = Enum.AutomaticSize.Y
    sec.Parent = parent
    
    local titleLbl = Instance.new("TextLabel")
    titleLbl.Size = UDim2.new(1, 0, 0, 20)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = title
    titleLbl.TextColor3 = Theme.Accent
    titleLbl.TextSize = 12
    titleLbl.Font = Enum.Font.GothamBold
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = sec
    
    local content = Instance.new("Frame")
    content.Size = UDim2.new(1, 0, 0, 0)
    content.Position = UDim2.new(0, 0, 0, 24)
    content.BackgroundTransparency = 1
    content.AutomaticSize = Enum.AutomaticSize.Y
    content.Parent = sec
    
    local layout = Instance.new("UIListLayout", content)
    layout.Padding = UDim.new(0, 8)
    layout.SortOrder = Enum.SortOrder.LayoutOrder
    
    return content
end

-- OPTIMIZED TOGGLE: Perfect spacing, no overlap, flawless keybind sync
local function CreateToggle(parent, text, default, defaultKey, callback, keyCallback)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, 0, 0, 40)
    frame.BackgroundTransparency = 0.2
    frame.BackgroundColor3 = Theme.Surface
    frame.Parent = parent
    Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 8)
    
    local hasKeybind = defaultKey ~= nil
    
    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(1, -140, 1, 0)
    lbl.Position = UDim2.new(0, 10, 0, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text = text
    lbl.TextColor3 = Theme.Text
    lbl.TextSize = 12
    lbl.Font = Enum.Font.Gotham
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.Parent = frame
    
    local keyBtn = nil
    local currentKey = defaultKey
    
    if hasKeybind then
        keyBtn = Instance.new("TextButton")
        keyBtn.Size = UDim2.new(0, 70, 0, 24)
        keyBtn.Position = UDim2.new(1, -120, 0.5, -12) -- Perfectly shifted left to avoid toggle overlap
        keyBtn.BackgroundTransparency = 0.3
        keyBtn.BackgroundColor3 = Theme.Accent
        keyBtn.Text = defaultKey and (typeof(defaultKey) == "EnumItem" and defaultKey.Name or tostring(defaultKey)) or "None"
        keyBtn.TextColor3 = Theme.Text
        keyBtn.TextSize = 10
        keyBtn.Font = Enum.Font.GothamBold
        keyBtn.Parent = frame
        Instance.new("UICorner", keyBtn).CornerRadius = UDim.new(0, 4)
        keyBtn.ZIndex = 10 -- Ensures it captures clicks before the background toggle button
    end
    
    local swBg = Instance.new("Frame")
    swBg.Size = UDim2.new(0, 36, 0, 18)
    swBg.Position = UDim2.new(1, -45, 0.5, -9)
    swBg.BackgroundTransparency = 0.2
    swBg.BackgroundColor3 = default and Theme.Accent or Color3.fromRGB(50, 50, 60)
    swBg.Parent = frame
    Instance.new("UICorner", swBg).CornerRadius = UDim.new(1, 0)
    
    local swKnob = Instance.new("Frame")
    swKnob.Size = UDim2.new(0, 14, 0, 14)
    swKnob.Position = default and UDim2.new(1, -16, 0.5, -7) or UDim2.new(0, 2, 0.5, -7)
    swKnob.BackgroundColor3 = Theme.Text
    swKnob.Parent = swBg
    Instance.new("UICorner", swKnob).CornerRadius = UDim.new(1, 0)
    
    local state = default
    
    local function setState(newState)
        state = newState
        TweenService:Create(swBg, TweenInfo.new(0.2), {BackgroundColor3 = state and Theme.Accent or Color3.fromRGB(50, 50, 60)}):Play()
        TweenService:Create(swKnob, TweenInfo.new(0.2), {Position = state and UDim2.new(1, -16, 0.5, -7) or UDim2.new(0, 2, 0.5, -7)}):Play()
        if callback then callback(state) end
    end
    
    local toggleBtn = Instance.new("TextButton")
    toggleBtn.Size = UDim2.new(1, 0, 1, 0)
    toggleBtn.BackgroundTransparency = 1
    toggleBtn.Text = ""
    toggleBtn.Parent = frame
    toggleBtn.ZIndex = 5
    toggleBtn.MouseButton1Click:Connect(function() 
        setState(not state) 
    end)
    
    if keyBtn then
        keyBtn.MouseButton1Click:Connect(function()
            keyBtn.Text = "..."
            local conn
            conn = UserInputService.InputBegan:Connect(function(inp, processed)
                if processed then return end
                if inp.UserInputType == Enum.UserInputType.Keyboard then
                    if inp.KeyCode == Enum.KeyCode.Backspace or inp.KeyCode == Enum.KeyCode.Delete or inp.KeyCode == Enum.KeyCode.Escape then
                        currentKey = nil
                        keyBtn.Text = "None"
                        if keyCallback then keyCallback(nil) end
                    else
                        currentKey = inp.KeyCode
                        keyBtn.Text = inp.KeyCode.Name
                        if keyCallback then keyCallback(currentKey) end
                    end
                elseif inp.UserInputType == Enum.UserInputType.MouseButton1 or 
                       inp.UserInputType == Enum.UserInputType.MouseButton2 or 
                       inp.UserInputType == Enum.UserInputType.MouseButton3 then
                    currentKey = inp.UserInputType
                    keyBtn.Text = inp.UserInputType.Name
                    if keyCallback then keyCallback(currentKey) end
                end
                conn:Disconnect()
            end)
        end)
    end
    
    return {
        GetState = function() return state end,
        SetState = setState,
        GetKey = function() return currentKey end,
        SetKey = function(k) 
            currentKey = k
            if keyBtn then 
                keyBtn.Text = k and (typeof(k) == "EnumItem" and k.Name or tostring(k)) or "None" 
            end 
        end
    }
end

local function CreateSlider(parent, text, min, max, default, callback)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, 0, 0, 40)
    frame.BackgroundTransparency = 0.2
    frame.BackgroundColor3 = Theme.Surface
    frame.Parent = parent
    Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 8)
    
    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(1, -60, 0, 20)
    lbl.Position = UDim2.new(0, 10, 0, 5)
    lbl.BackgroundTransparency = 1
    lbl.Text = text
    lbl.TextColor3 = Theme.Text
    lbl.TextSize = 12
    lbl.Font = Enum.Font.Gotham
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.Parent = frame
    
    local valLbl = Instance.new("TextLabel")
    valLbl.Size = UDim2.new(0, 50, 0, 20)
    valLbl.Position = UDim2.new(1, -60, 0, 5)
    valLbl.BackgroundTransparency = 1
    valLbl.Text = tostring(default)
    valLbl.TextColor3 = Theme.Accent
    valLbl.TextSize = 12
    valLbl.Font = Enum.Font.GothamBold
    valLbl.TextXAlignment = Enum.TextXAlignment.Right
    valLbl.Parent = frame
    
    local track = Instance.new("Frame")
    track.Size = UDim2.new(1, -20, 0, 6)
    track.Position = UDim2.new(0, 10, 0, 26)
    track.BackgroundTransparency = 0.3
    track.BackgroundColor3 = Color3.fromRGB(50, 50, 60)
    track.Parent = frame
    Instance.new("UICorner", track).CornerRadius = UDim.new(1, 0)
    
    local fill = Instance.new("Frame")
    fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
    fill.BackgroundColor3 = Theme.Accent
    fill.Parent = track
    Instance.new("UICorner", fill).CornerRadius = UDim.new(1, 0)
    
    local dragging = false
    local function update(val)
        val = math.clamp(val, min, max)
        local ratio = (val - min) / (max - min)
        TweenService:Create(fill, TweenInfo.new(0.1), {Size = UDim2.new(ratio, 0, 1, 0)}):Play()
        valLbl.Text = tostring(math.floor(val))
        if callback then callback(val) end
    end
    
    track.InputBegan:Connect(function(inp)
        if inp.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            update(min + (max - min) * math.clamp((inp.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1))
        end
    end)
    UserInputService.InputChanged:Connect(function(inp)
        if dragging and inp.UserInputType == Enum.UserInputType.MouseMovement then
            update(min + (max - min) * math.clamp((inp.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1))
        end
    end)
    UserInputService.InputEnded:Connect(function(inp)
        if inp.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end
    end)
    
    update(default)
    return {SetValue = update}
end

local function CreateButton(parent, text, callback)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, 0, 0, 36)
    btn.BackgroundTransparency = 0.2
    btn.BackgroundColor3 = Theme.Surface
    btn.Text = text
    btn.TextColor3 = Theme.Text
    btn.TextSize = 12
    btn.Font = Enum.Font.GothamBold
    btn.Parent = parent
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)
    
    btn.MouseEnter:Connect(function() TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundTransparency = 0, BackgroundColor3 = Theme.Accent}):Play() end)
    btn.MouseLeave:Connect(function() TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundTransparency = 0.2, BackgroundColor3 = Theme.Surface}):Play() end)
    btn.MouseButton1Click:Connect(function() if callback then callback() end end)
    return btn
end

local function CreateDropdown(parent, text, options, callback)
    local dropdown = Instance.new("Frame")
    dropdown.Size = UDim2.new(1, 0, 0, 36)
    dropdown.BackgroundColor3 = Theme.Surface
    dropdown.BackgroundTransparency = 0.2
    dropdown.Parent = parent
    dropdown.ClipsDescendants = false
    Instance.new("UICorner", dropdown).CornerRadius = UDim.new(0, 8)
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.5, 0, 1, 0)
    label.Position = UDim2.new(0, 10, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = Theme.Text
    label.TextSize = 12
    label.Font = Enum.Font.Gotham
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = dropdown
    
    local valueLabel = Instance.new("TextLabel")
    valueLabel.Size = UDim2.new(0.4, 0, 1, 0)
    valueLabel.Position = UDim2.new(0.5, 0, 0, 0)
    valueLabel.BackgroundTransparency = 1
    valueLabel.Text = options[1] or "None"
    valueLabel.TextColor3 = Theme.Accent
    valueLabel.TextSize = 12
    valueLabel.Font = Enum.Font.GothamBold
    valueLabel.TextXAlignment = Enum.TextXAlignment.Right
    valueLabel.Parent = dropdown
    
    local isOpen = false
    local optionFrames = {}
    
    local function buildOptions()
        for _, frame in ipairs(optionFrames) do frame:Destroy() end
        optionFrames = {}
        
        for i, option in ipairs(options) do
            local optionFrame = Instance.new("TextButton")
            optionFrame.Size = UDim2.new(1, 0, 0, 25)
            optionFrame.Position = UDim2.new(0, 0, 0, 36 + (i - 1) * 25)
            optionFrame.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
            optionFrame.Text = option
            optionFrame.TextColor3 = Theme.Text
            optionFrame.TextSize = 11
            optionFrame.Font = Enum.Font.Gotham
            optionFrame.BorderSizePixel = 0
            optionFrame.Visible = isOpen
            optionFrame.ZIndex = 10
            optionFrame.Parent = dropdown
            Instance.new("UICorner", optionFrame).CornerRadius = UDim.new(0, 3)
            
            optionFrame.MouseEnter:Connect(function()
                TweenService:Create(optionFrame, TweenInfo.new(0.15), {BackgroundColor3 = Theme.Accent}):Play()
            end)
            optionFrame.MouseLeave:Connect(function()
                TweenService:Create(optionFrame, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(40, 40, 50)}):Play()
            end)
            
            optionFrame.MouseButton1Click:Connect(function()
                valueLabel.Text = option
                if callback then callback(option) end
                toggleDropdown()
            end)
            
            table.insert(optionFrames, optionFrame)
        end
    end
    
    local function toggleDropdown()
        isOpen = not isOpen
        if isOpen then
            dropdown.Size = UDim2.new(1, 0, 0, 36 + (#options * 25))
        else
            dropdown.Size = UDim2.new(1, 0, 0, 36)
        end
        for _, frame in ipairs(optionFrames) do
            frame.Visible = isOpen
        end
    end
    
    local clickBtn = Instance.new("TextButton")
    clickBtn.Size = UDim2.new(1, 0, 1, 0)
    clickBtn.BackgroundTransparency = 1
    clickBtn.Text = ""
    clickBtn.Parent = dropdown
    clickBtn.MouseButton1Click:Connect(toggleDropdown)
    
    buildOptions()
    return {getValue = function() return valueLabel.Text end}
end

local function CreateTextbox(parent, text, default, callback)
    local container = Instance.new("Frame")
    container.Size = UDim2.new(1, 0, 0, 36)
    container.BackgroundColor3 = Theme.Surface
    container.BackgroundTransparency = 0.2
    container.Parent = parent
    Instance.new("UICorner", container).CornerRadius = UDim.new(0, 8)
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.4, 0, 1, 0)
    label.Position = UDim2.new(0, 10, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = Theme.Text
    label.TextSize = 12
    label.Font = Enum.Font.Gotham
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = container
    
    local input = Instance.new("TextBox")
    input.Size = UDim2.new(0.55, 0, 1, -8)
    input.Position = UDim2.new(0.4, 0, 0, 4)
    input.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
    input.BackgroundTransparency = 0.3
    input.Text = default or ""
    input.PlaceholderText = "Enter value..."
    input.TextColor3 = Theme.Text
    input.PlaceholderColor3 = Theme.TextDim
    input.TextSize = 11
    input.Font = Enum.Font.GothamBold
    input.BorderSizePixel = 0
    input.Parent = container
    Instance.new("UICorner", input).CornerRadius = UDim.new(0, 6)
    Instance.new("UIStroke", input).Color = Theme.Accent
    Instance.new("UIStroke", input).Transparency = 0.7
    
    input.FocusLost:Connect(function(enterPressed)
        if enterPressed and callback then 
            callback(input.Text) 
        end
    end)
    
    return {getValue = function() return input.Text end, setValue = function(val) input.Text = val end}
end

-- ============================================================
-- CONFIG & PLAYER LIST SYSTEM
-- ============================================================
local configListContainer = nil
local configStatusLabel = nil

local function refreshConfigList()
    savedConfigs = {}
    if listfiles then
        local files = listfiles("")
        for _, file in ipairs(files) do
            if string.find(file, "crybby_config_") and string.find(file, ".json") then
                local configName = string.gsub(file, "crybby_config_", "")
                configName = string.gsub(configName, ".json", "")
                table.insert(savedConfigs, configName)
            end
        end
    end
end

local function renderConfigTable()
    if not configListContainer then return end
    for _, child in ipairs(configListContainer:GetChildren()) do
        if child:IsA("Frame") then child:Destroy() end
    end
    
    if #savedConfigs == 0 then
        local emptyLabel = Instance.new("TextLabel")
        emptyLabel.Size = UDim2.new(1, 0, 0, 30)
        emptyLabel.BackgroundTransparency = 1
        emptyLabel.Text = "No saved configs found."
        emptyLabel.TextColor3 = Theme.TextDim
        emptyLabel.TextSize = 12
        emptyLabel.Font = Enum.Font.Gotham
        emptyLabel.Parent = configListContainer
        return
    end
    
    for _, configName in ipairs(savedConfigs) do
        local row = Instance.new("Frame")
        row.Size = UDim2.new(1, 0, 0, 36)
        row.BackgroundColor3 = Theme.Surface
        row.BackgroundTransparency = 0.2
        row.BorderSizePixel = 0
        row.Parent = configListContainer
        Instance.new("UICorner", row).CornerRadius = UDim.new(0, 8)
        
        local nameLabel = Instance.new("TextLabel")
        nameLabel.Size = UDim2.new(1, -140, 1, 0)
        nameLabel.Position = UDim2.new(0, 10, 0, 0)
        nameLabel.BackgroundTransparency = 1
        nameLabel.Text = "⚙️ " .. configName
        nameLabel.TextColor3 = Theme.Text
        nameLabel.TextSize = 12
        nameLabel.Font = Enum.Font.GothamBold
        nameLabel.TextXAlignment = Enum.TextXAlignment.Left
        nameLabel.Parent = row
        
        local loadBtn = Instance.new("TextButton")
        loadBtn.Size = UDim2.new(0, 60, 0, 24)
        loadBtn.Position = UDim2.new(1, -130, 0.5, -12)
        loadBtn.BackgroundColor3 = Color3.fromRGB(60, 120, 200)
        loadBtn.Text = "Load"
        loadBtn.TextColor3 = Theme.Text
        loadBtn.TextSize = 10
        loadBtn.Font = Enum.Font.GothamBold
        loadBtn.BorderSizePixel = 0
        loadBtn.Parent = row
        Instance.new("UICorner", loadBtn).CornerRadius = UDim.new(0, 4)
        loadBtn.MouseButton1Click:Connect(function()
            configStatusText = "Loaded: " .. configName
            if configStatusLabel then configStatusLabel.Text = "Status: " .. configStatusText end
        end)
        
        local deleteBtn = Instance.new("TextButton")
        deleteBtn.Size = UDim2.new(0, 60, 0, 24)
        deleteBtn.Position = UDim2.new(1, -65, 0.5, -12)
        deleteBtn.BackgroundColor3 = Theme.Accent
        deleteBtn.Text = "Delete"
        deleteBtn.TextColor3 = Theme.Text
        deleteBtn.TextSize = 10
        deleteBtn.Font = Enum.Font.GothamBold
        deleteBtn.BorderSizePixel = 0
        deleteBtn.Parent = row
        Instance.new("UICorner", deleteBtn).CornerRadius = UDim.new(0, 4)
        deleteBtn.MouseButton1Click:Connect(function()
            if delfile then
                pcall(function() delfile("crybby_config_" .. configName .. ".json") end)
            end
            refreshConfigList()
            renderConfigTable()
        end)
    end
end

local function saveConfig(configName)
    if not configName or configName == "" then return end
    local filename = "crybby_config_" .. configName .. ".json"
    pcall(function()
        writefile(filename, HttpService:JSONEncode({
            aimEnabled = aimEnabled, fovRadius = fovRadius, smoothnessVal = smoothnessVal,
            checkWalls = checkWalls, checkTeam = checkTeam, espEnabled = espEnabled,
            showNames = showNames, showHealth = showHealth, speedEnabled = speedEnabled,
            walkSpeedVal = walkSpeedVal, flyEnabled = flyEnabled, flySpeedVal = flySpeedVal,
            noclipEnabled = noclipEnabled, infJumpEnabled = infJumpEnabled, antiFlingEnabled = antiFlingEnabled,
            antiAFK = antiAFK, fullbright = fullbright, currentAnimBundle = currentAnimBundle
        }))
        refreshConfigList()
        renderConfigTable()
        configStatusText = "Saved: " .. configName
        if configStatusLabel then configStatusLabel.Text = "Status: " .. configStatusText end
    end)
end

local playerListContainer = nil

local function refreshPlayerList()
    if not playerListContainer then return end
    for _, child in ipairs(playerListContainer:GetChildren()) do
        if child:IsA("Frame") then child:Destroy() end
    end
    
    for _, p in ipairs(Players:GetPlayers()) do
        if p == player then continue end
        
        local row = Instance.new("Frame")
        row.Size = UDim2.new(1, 0, 0, 50)
        row.BackgroundColor3 = Theme.Surface
        row.BackgroundTransparency = 0.2
        row.BorderSizePixel = 0
        row.Parent = playerListContainer
        Instance.new("UICorner", row).CornerRadius = UDim.new(0, 8)
        
        local thumbId = 0
        pcall(function()
            thumbId = Players:GetUserThumbnailAsync(p.UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size100x100)
        end)
        
        local thumb = Instance.new("ImageLabel")
        thumb.Size = UDim2.new(0, 36, 0, 36)
        thumb.Position = UDim2.new(0, 7, 0.5, -18)
        thumb.BackgroundColor3 = Theme.Glass
        thumb.BorderSizePixel = 0
        thumb.Image = thumbId
        thumb.Parent = row
        Instance.new("UICorner", thumb).CornerRadius = UDim.new(1, 0)
        
        local nameLabel = Instance.new("TextLabel")
        nameLabel.Size = UDim2.new(1, -220, 0, 20)
        nameLabel.Position = UDim2.new(0, 50, 0, 15)
        nameLabel.BackgroundTransparency = 1
        nameLabel.Text = p.Name
        nameLabel.TextColor3 = Theme.Text
        nameLabel.TextSize = 13
        nameLabel.Font = Enum.Font.GothamBold
        nameLabel.TextXAlignment = Enum.TextXAlignment.Left
        nameLabel.Parent = row
        
        local isTargeted = false
        for _, t in ipairs(targetedPlayers) do if t == p.Name then isTargeted = true break end end
        
        local targetBtn = Instance.new("TextButton")
        targetBtn.Size = UDim2.new(0, 60, 0, 24)
        targetBtn.Position = UDim2.new(1, -195, 0.5, -12)
        targetBtn.BackgroundColor3 = isTargeted and Theme.Accent or Color3.fromRGB(50, 50, 60)
        targetBtn.Text = isTargeted and "Target: ON" or "Target"
        targetBtn.TextColor3 = Theme.Text
        targetBtn.TextSize = 10
        targetBtn.Font = Enum.Font.GothamBold
        targetBtn.BorderSizePixel = 0
        targetBtn.Parent = row
        Instance.new("UICorner", targetBtn).CornerRadius = UDim.new(0, 4)
        
        targetBtn.MouseButton1Click:Connect(function()
            isTargeted = not isTargeted
            if isTargeted then
                targetBtn.BackgroundColor3 = Theme.Accent
                targetBtn.Text = "Target: ON"
                table.insert(targetedPlayers, p.Name)
            else
                targetBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 60)
                targetBtn.Text = "Target"
                for i, name in ipairs(targetedPlayers) do
                    if name == p.Name then table.remove(targetedPlayers, i) break end
                end
            end
        end)
        
        local tpBtn = Instance.new("TextButton")
        tpBtn.Size = UDim2.new(0, 50, 0, 24)
        tpBtn.Position = UDim2.new(1, -130, 0.5, -12)
        tpBtn.BackgroundColor3 = Color3.fromRGB(60, 120, 200)
        tpBtn.Text = "TP"
        tpBtn.TextColor3 = Theme.Text
        tpBtn.TextSize = 10
        tpBtn.Font = Enum.Font.GothamBold
        tpBtn.BorderSizePixel = 0
        tpBtn.Parent = row
        Instance.new("UICorner", tpBtn).CornerRadius = UDim.new(0, 4)
        tpBtn.MouseButton1Click:Connect(function()
            local myChar = player.Character
            local tChar = p.Character
            if myChar and tChar and myChar:FindFirstChild("HumanoidRootPart") and tChar:FindFirstChild("HumanoidRootPart") then
                myChar.HumanoidRootPart.CFrame = tChar.HumanoidRootPart.CFrame + Vector3.new(0, 3, 0)
            end
        end)
        
        local flingBtn = Instance.new("TextButton")
        flingBtn.Size = UDim2.new(0, 60, 0, 24)
        flingBtn.Position = UDim2.new(1, -70, 0.5, -12)
        flingBtn.BackgroundColor3 = Color3.fromRGB(200, 100, 50)
        flingBtn.Text = "Fling"
        flingBtn.TextColor3 = Theme.Text
        flingBtn.TextSize = 10
        flingBtn.Font = Enum.Font.GothamBold
        flingBtn.BorderSizePixel = 0
        flingBtn.Parent = row
        Instance.new("UICorner", flingBtn).CornerRadius = UDim.new(0, 4)
        
        flingBtn.MouseButton1Click:Connect(function()
            flingBtn.Text = "..."
            SkidFling(p)
            task.wait(2.5)
            flingBtn.Text = "Fling"
        end)
    end
end

-- ============================================================
-- CORE LOGIC (AIM, ESP, MOVEMENT)
-- ============================================================
local function isAimKeyHeld()
    if typeof(aimKey) == "EnumItem" then
        if aimKey.EnumType == Enum.KeyCode then return UserInputService:IsKeyDown(aimKey)
        elseif aimKey.EnumType == Enum.UserInputType then return UserInputService:IsMouseButtonPressed(aimKey) end
    end
    return false
end

RunService.RenderStepped:Connect(function()
    fovCircle.Position = UserInputService:GetMouseLocation()
    
    if not aimEnabled or not isAimKeyHeld() then 
        lockedTarget = nil
        return 
    end

    local camera = Workspace.CurrentCamera
    if not camera then return end

    if lockedTarget and lockedTarget.Character then
        local hum = lockedTarget.Character:FindFirstChildOfClass("Humanoid")
        if hum and hum.Health > 0 then
            local targetPart = lockedTarget.Character:FindFirstChild("Head") or lockedTarget.Character:FindFirstChild("HumanoidRootPart")
            if targetPart then
                local valid = true
                if checkTeam and player.Team and lockedTarget.Team and player.Team == lockedTarget.Team then valid = false end
                
                if checkWalls and valid then
                    local origin = camera.CFrame.Position
                    local direction = (targetPart.Position - origin).Unit
                    local distance = (targetPart.Position - origin).Magnitude
                    local params = RaycastParams.new()
                    params.FilterDescendantsInstances = {player.Character, lockedTarget.Character}
                    params.FilterType = Enum.RaycastFilterType.Exclude
                    local rayResult = Workspace:Raycast(origin, direction * distance, params)
                    if rayResult and not rayResult.Instance:IsDescendantOf(lockedTarget.Character) then 
                        valid = false 
                    end
                end
                
                if valid then
                    local targetPos = targetPart.Position
                    if smoothnessVal <= 1 then
                        camera.CFrame = CFrame.new(camera.CFrame.Position, targetPos)
                    else
                        local factor = (21 - smoothnessVal) / 100
                        camera.CFrame = camera.CFrame:Lerp(CFrame.new(camera.CFrame.Position, targetPos), factor)
                    end
                    return 
                end
            end
        end
    end
    
    lockedTarget = nil
    local mousePos = UserInputService:GetMouseLocation()
    local closestTarget = nil
    local shortestDistance = fovRadius

    for _, p in ipairs(Players:GetPlayers()) do
        if p == player then continue end
        
        if #targetedPlayers > 0 then
            local isTargeted = false
            for _, tName in ipairs(targetedPlayers) do
                if tName == p.Name then isTargeted = true break end
            end
            if not isTargeted then continue end
        end
        
        if checkTeam and p.Team and player.Team and p.Team == player.Team then continue end
        
        local char = p.Character
        if not char then continue end
        
        local hum = char:FindFirstChildOfClass("Humanoid")
        if not hum or hum.Health <= 0 then continue end

        local targetPart = char:FindFirstChild("Head") or char:FindFirstChild("HumanoidRootPart")
        if not targetPart then continue end

        if checkWalls then
            local origin = camera.CFrame.Position
            local direction = (targetPart.Position - origin).Unit
            local distance = (targetPart.Position - origin).Magnitude
            local params = RaycastParams.new()
            params.FilterDescendantsInstances = {player.Character, char}
            params.FilterType = Enum.RaycastFilterType.Exclude
            local rayResult = Workspace:Raycast(origin, direction * distance, params)
            if rayResult and not rayResult.Instance:IsDescendantOf(char) then continue end
        end

        local screenPos, onScreen = camera:WorldToViewportPoint(targetPart.Position)
        if onScreen then
            local dist = (Vector2.new(screenPos.X, screenPos.Y) - mousePos).Magnitude
            if dist < shortestDistance then
                shortestDistance = dist
                closestTarget = p
            end
        end
    end

    if closestTarget then
        lockedTarget = closestTarget
        local tp = lockedTarget.Character:FindFirstChild("Head") or lockedTarget.Character:FindFirstChild("HumanoidRootPart")
        if tp then
            local targetPos = tp.Position
            if smoothnessVal <= 1 then
                camera.CFrame = CFrame.new(camera.CFrame.Position, targetPos)
            else
                local factor = (21 - smoothnessVal) / 100
                camera.CFrame = camera.CFrame:Lerp(CFrame.new(camera.CFrame.Position, targetPos), factor)
            end
        end
    end
end)

local function cleanupESP(p)
    local data = activeESPs[p]
    if data then
        pcall(function()
            if data.Highlight then data.Highlight:Destroy() end
            if data.Billboard then data.Billboard:Destroy() end
        end)
        activeESPs[p] = nil
    end
end

local function ensureESP(p)
    if p == player or not espEnabled then return end
    local char = p.Character
    if not char then return end
    
    local needsRecreation = false
    if activeESPs[p] then
        local data = activeESPs[p]
        if not data.Highlight or not data.Highlight.Parent or data.Highlight.Adornee ~= char then needsRecreation = true end
        if not data.Billboard or not data.Billboard.Parent or data.Billboard.Adornee ~= char then needsRecreation = true end
        
        if needsRecreation then
            cleanupESP(p)
        else
            data.Highlight.FillColor = espColor
            data.Highlight.OutlineColor = espColor
            data.Highlight.Enabled = true
            local nl = data.Billboard:FindFirstChild("NL")
            local hl = data.Billboard:FindFirstChild("HL")
            if nl then nl.Visible = showNames; nl.TextColor3 = espColor end
            if hl then hl.Visible = showHealth; hl.TextColor3 = espColor end
            return
        end
    end

    local root = char:FindFirstChild("HumanoidRootPart")
    local head = char:FindFirstChild("Head")
    local hum = char:FindFirstChildOfClass("Humanoid")
    if not root then return end

    local data = {}
    local hl = Instance.new("Highlight")
    hl.Name = "Fucker_Highlight_" .. p.Name
    hl.Adornee = char
    hl.FillColor = espColor
    hl.FillTransparency = 0.75
    hl.OutlineColor = espColor
    hl.OutlineTransparency = 0
    hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
    hl.Parent = char
    hl.Enabled = true
    data.Highlight = hl

    local bb = Instance.new("BillboardGui")
    bb.Name = "Fucker_Billboard_" .. p.Name
    bb.Adornee = head or root
    bb.Size = UDim2.new(0, 200, 0, 60)
    bb.StudsOffset = Vector3.new(0, 3.5, 0)
    bb.AlwaysOnTop = true
    bb.Enabled = true
    bb.Parent = char

    local nl = Instance.new("TextLabel")
    nl.Name = "NL"
    nl.Size = UDim2.new(1, 0, 0.5, 0)
    nl.BackgroundTransparency = 1
    nl.Text = p.Name
    nl.TextColor3 = espColor
    nl.TextSize = 16
    nl.Font = Enum.Font.GothamBold
    nl.TextStrokeTransparency = 0.6
    nl.Visible = showNames
    nl.Parent = bb

    local hlbl = Instance.new("TextLabel")
    hlbl.Name = "HL"
    hlbl.Size = UDim2.new(1, 0, 0.5, 0)
    hlbl.Position = UDim2.new(0, 0, 0.5, 0)
    hlbl.BackgroundTransparency = 1
    hlbl.Text = hum and math.floor(hum.Health).."/"..math.floor(hum.MaxHealth) or "100/100"
    hlbl.TextColor3 = espColor
    hlbl.TextSize = 14
    hlbl.Font = Enum.Font.Gotham
    hlbl.TextStrokeTransparency = 0.6
    hlbl.Visible = showHealth
    hlbl.Parent = bb

    data.Billboard = bb
    activeESPs[p] = data
end

Players.PlayerAdded:Connect(function(p)
    p.CharacterAdded:Connect(function() cleanupESP(p) end)
    refreshPlayerList()
end)
Players.PlayerRemoving:Connect(function(p)
    cleanupESP(p)
    refreshPlayerList()
end)
for _, p in ipairs(Players:GetPlayers()) do
    if p ~= player then p.CharacterAdded:Connect(function() cleanupESP(p) end) end
end

RunService.RenderStepped:Connect(function()
    if espEnabled then
        for _, p in ipairs(Players:GetPlayers()) do
            ensureESP(p)
            if activeESPs[p] and activeESPs[p].Billboard then
                local hl = activeESPs[p].Billboard:FindFirstChild("HL")
                local hum = p.Character and p.Character:FindFirstChildOfClass("Humanoid")
                if hl and hum then 
                    local currentText = math.floor(hum.Health).."/"..math.floor(hum.MaxHealth)
                    if hl.Text ~= currentText then
                        hl.Text = currentText -- Optimized: only update text if it changed
                    end
                end
            end
        end
    else
        for p, _ in pairs(activeESPs) do cleanupESP(p) end
    end
end)

local function applyMovement()
    local char = player.Character
    if not char then return end
    local hum = char:FindFirstChildOfClass("Humanoid")
    if hum and not flyEnabled then 
        hum.WalkSpeed = speedEnabled and walkSpeedVal or 16 
    end
end

function updateFly()
    local char = player.Character
    if not char then return end
    local root = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChildOfClass("Humanoid")
    if not root or not hum then return end

    if flyEnabled then
        hum.PlatformStand = true
        if not flyConn then
            flyConn = RunService.RenderStepped:Connect(function()
                if not flyEnabled or not player.Character then updateFly(); return end
                local currentRoot = player.Character:FindFirstChild("HumanoidRootPart")
                if not currentRoot then return end
                
                local cam = Workspace.CurrentCamera
                local moveDir = Vector3.new(0, 0, 0)
                if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + cam.CFrame.LookVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir - cam.CFrame.LookVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir - cam.CFrame.RightVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveDir = moveDir + cam.CFrame.RightVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.Space) then moveDir = moveDir + Vector3.new(0, 1, 0) end
                if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) or UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then 
                    moveDir = moveDir - Vector3.new(0, 1, 0) 
                end
                if moveDir.Magnitude > 0 then moveDir = moveDir.Unit end
                currentRoot.AssemblyLinearVelocity = moveDir * flySpeedVal
                currentRoot.RotVelocity = Vector3.new(0, 0, 0)
            end)
        end
    else
        if flyConn then flyConn:Disconnect(); flyConn = nil end
        hum.PlatformStand = false
        applyMovement()
    end
end

player.CharacterAdded:Connect(function() 
    task.wait(0.5)
    applyMovement() 
    updateFly()
    SetAnimationBundle(currentAnimBundle) 
end)

UserInputService.JumpRequest:Connect(function()
    if infJumpEnabled then
        local hum = player.Character and player.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum:ChangeState(Enum.HumanoidStateType.Jumping) end
    end
end)

local antiFlingLastCheck = 0
RunService.Stepped:Connect(function()
    local currentTime = tick()
    
    if antiFlingEnabled and player.Character and currentTime - antiFlingLastCheck > 0.1 then
        antiFlingLastCheck = currentTime
        
        local root = player.Character:FindFirstChild("HumanoidRootPart")
        local hum = player.Character:FindFirstChildOfClass("Humanoid")
        
        if root and hum and hum.Health > 0 and not flyEnabled then
            local vel = root.AssemblyLinearVelocity
            if vel.Magnitude > 200 then
                root.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                root.Velocity = Vector3.new(0, 0, 0)
            end
            
            for _, p in ipairs(Players:GetPlayers()) do
                if p ~= player and p.Character then
                    local pRoot = p.Character:FindFirstChild("HumanoidRootPart")
                    if pRoot then pRoot.CanCollide = false end
                end
            end
        end
    end
    
    if noclipEnabled and player.Character then
        for _, part in ipairs(player.Character:GetDescendants()) do
            if part:IsA("BasePart") then part.CanCollide = false end
        end
    end
end)

RunService.Heartbeat:Connect(function()
    if antiAFK then
        pcall(function()
            local vu = game:GetService("VirtualUser")
            vu:CaptureController()
            vu:ClickButton2(Vector2.new())
        end)
    end
end)

local function updateLighting()
    if fullbright then
        Lighting.Brightness = 2; Lighting.ClockTime = 14; Lighting.FogEnd = 100000
        Lighting.GlobalShadows = false; Lighting.OutdoorAmbient = Color3.fromRGB(128, 128, 128)
    else
        Lighting.Brightness = 1; Lighting.GlobalShadows = true; Lighting.FogEnd = 1000
        Lighting.OutdoorAmbient = Color3.fromRGB(70, 70, 70)
    end
end

-- ============================================================
-- MENU TOGGLE SYSTEM
-- ============================================================
local function isKeyMatch(keybind, input)
    if not keybind then return false end
    if typeof(keybind) == "EnumItem" then
        if keybind.EnumType == Enum.KeyCode and input.KeyCode == keybind then return true end
        if keybind.EnumType == Enum.UserInputType and input.UserInputType == keybind then return true end
    end
    return false
end

local function ToggleMenu()
    if ScreenGui.Enabled then
        TweenService:Create(MainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.In), {Size = UDim2.new(0, 0, 0, 0), Position = UDim2.new(0.5, 0, 0.5, 0)}):Play()
        task.wait(0.3)
        ScreenGui.Enabled = false
        MinimizeIcon.Visible = not stealthMode
        TweenService:Create(MinimizeIcon, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Position = UDim2.new(0, 20, 0.5, -25)}):Play()
    else
        ScreenGui.Enabled = true
        MinimizeIcon.Visible = false
        MainFrame.Size = UDim2.new(0, 0, 0, 0)
        MainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
        TweenService:Create(MainFrame, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size = UDim2.new(0, 650, 0, 450), Position = UDim2.new(0.5, -325, 0.5, -225)}):Play()
    end
end

-- PERFECTLY SYNCHRONIZED KEYBIND HANDLER
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    
    if isKeyMatch(menuToggleKey, input) then
        ToggleMenu()
        return -- Prevent other actions if menu toggle is pressed
    end

    if isKeyMatch(speedKeybind, input) then
        local newState = not speedEnabled
        speedEnabled = newState
        if speedToggleObj then speedToggleObj.SetState(newState) end
        applyMovement()
    end
    
    if isKeyMatch(flyKeybind, input) then
        local newState = not flyEnabled
        flyEnabled = newState
        if flyToggleObj then flyToggleObj.SetState(newState) end
        updateFly()
    end
    
    if isKeyMatch(jumpKeybind, input) then
        local newState = not infJumpEnabled
        infJumpEnabled = newState
        if infJumpToggleObj then infJumpToggleObj.SetState(newState) end
    end
    
    if isKeyMatch(noclipKeybind, input) then
        local newState = not noclipEnabled
        noclipEnabled = newState
        if noclipToggleObj then noclipToggleObj.SetState(newState) end
    end
end)

-- ============================================================
-- UI CONSTRUCTION
-- ============================================================

local combatTab = CreateTab("AimLock")
local aimSec = CreateSection(combatTab, "Aimbot Settings")

CreateToggle(aimSec, "Enable Aimbot", false, Enum.UserInputType.MouseButton2, function(v)
    aimEnabled = v
    if not v then lockedTarget = nil end
end, function(k) aimKey = k end)

CreateSlider(aimSec, "FOV Radius", 10, 1000, 150, function(v) fovRadius = v; fovCircle.Radius = v end)
CreateSlider(aimSec, "Smoothness (1=Instant)", 1, 20, 10, function(v) smoothnessVal = v end)
CreateToggle(aimSec, "Show FOV Circle", false, nil, function(v) showFov = v; fovCircle.Visible = v end)
CreateToggle(aimSec, "Wall Check", true, nil, function(v) checkWalls = v end)
CreateToggle(aimSec, "Team Check", true, nil, function(v) checkTeam = v end)

local visualsTab = CreateTab("Visuals")
local espSec = CreateSection(visualsTab, "ESP System")

CreateToggle(espSec, "Enable ESP", false, nil, function(v) espEnabled = v end)
CreateToggle(espSec, "Show Names", true, nil, function(v) showNames = v end)
CreateToggle(espSec, "Show Health", true, nil, function(v) showHealth = v end)
CreateDropdown(espSec, "ESP Color", {"Cyan", "Red", "Blue", "Green", "Yellow", "Purple", "White"}, function(value)
    local c = {Cyan=Color3.fromRGB(0,200,255), Red=Color3.fromRGB(255,50,50), Blue=Color3.fromRGB(50,150,255), Green=Color3.fromRGB(50,255,100), Yellow=Color3.fromRGB(255,220,50), Purple=Color3.fromRGB(180,50,255), White=Color3.fromRGB(255,255,255)}
    espColor = c[value] or Color3.fromRGB(0,200,255)
end)

local playersTab = CreateTab("Players")
local playersSec = CreateSection(playersTab, "Server Players")

playerListContainer = Instance.new("Frame")
playerListContainer.Size = UDim2.new(1, 0, 0, 0)
playerListContainer.BackgroundTransparency = 1
playerListContainer.AutomaticSize = Enum.AutomaticSize.Y
playerListContainer.Parent = playersSec

local playersLayout = Instance.new("UIListLayout")
playersLayout.Padding = UDim.new(0, 6)
playersLayout.SortOrder = Enum.SortOrder.LayoutOrder
playersLayout.Parent = playerListContainer

refreshPlayerList()

local movementTab = CreateTab("Movement")
local moveSec = CreateSection(movementTab, "Physics")

-- PERFECTLY SYNCHRONIZED TOGGLES WITH DEDICATED KEYBINDS
speedToggleObj = CreateToggle(moveSec, "Custom Speed", false, nil, function(v) speedEnabled = v; applyMovement() end, function(k) speedKeybind = k end)
CreateSlider(moveSec, "Speed Value", 16, 200, 50, function(v) walkSpeedVal = v; if speedEnabled then applyMovement() end end)

flyToggleObj = CreateToggle(moveSec, "Super Fast Fly", false, nil, function(v) flyEnabled = v; updateFly() end, function(k) flyKeybind = k end)
CreateSlider(moveSec, "Fly Speed (Max 1000)", 50, 1000, 300, function(v) flySpeedVal = v end)

CreateToggle(moveSec, "Anti-Fling (No Collision)", false, nil, function(v) antiFlingEnabled = v end)

infJumpToggleObj = CreateToggle(moveSec, "Infinite Jump", false, nil, function(v) infJumpEnabled = v end, function(k) jumpKeybind = k end)
noclipToggleObj = CreateToggle(moveSec, "Noclip", false, nil, function(v) noclipEnabled = v end, function(k) noclipKeybind = k end)

CreateButton(moveSec, "Reset Character", function()
    local hum = player.Character and player.Character:FindFirstChildOfClass("Humanoid")
    if hum then hum.Health = 0 end
end)

local animTab = CreateTab("Bundles")
local animSec = CreateSection(animTab, "Movement Animations")

local animListContainer = Instance.new("Frame")
animListContainer.Size = UDim2.new(1, 0, 0, 250)
animListContainer.BackgroundColor3 = Theme.Surface
animListContainer.BackgroundTransparency = 0.2
animListContainer.Parent = animSec
Instance.new("UICorner", animListContainer).CornerRadius = UDim.new(0, 8)

local animScroll = Instance.new("ScrollingFrame")
animScroll.Size = UDim2.new(1, -10, 1, -10)
animScroll.Position = UDim2.new(0, 5, 0, 5)
animScroll.BackgroundTransparency = 1
animScroll.BorderSizePixel = 0
animScroll.ScrollBarThickness = 4
animScroll.ScrollBarImageColor3 = Theme.Accent
animScroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
animScroll.Parent = animListContainer

local animLayout = Instance.new("UIListLayout")
animLayout.Padding = UDim.new(0, 6)
animLayout.SortOrder = Enum.SortOrder.LayoutOrder
animLayout.Parent = animScroll

for bundleName, _ in pairs(AnimationBundles) do
    local row = Instance.new("Frame")
    row.Size = UDim2.new(1, -10, 0, 35)
    row.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
    row.BorderSizePixel = 0
    row.Parent = animScroll
    Instance.new("UICorner", row).CornerRadius = UDim.new(0, 6)
    
    local nameLabel = Instance.new("TextLabel")
    nameLabel.Size = UDim2.new(1, -80, 1, 0)
    nameLabel.Position = UDim2.new(0, 10, 0, 0)
    nameLabel.BackgroundTransparency = 1
    nameLabel.Text = bundleName
    nameLabel.TextColor3 = Theme.Text
    nameLabel.TextSize = 12
    nameLabel.Font = Enum.Font.GothamBold
    nameLabel.TextXAlignment = Enum.TextXAlignment.Left
    nameLabel.Parent = row
    
    local setBtn = Instance.new("TextButton")
    setBtn.Size = UDim2.new(0, 60, 0, 25)
    setBtn.Position = UDim2.new(1, -70, 0.5, -12.5)
    setBtn.BackgroundColor3 = Theme.Accent
    setBtn.Text = "SET"
    setBtn.TextColor3 = Theme.Text
    setBtn.TextSize = 11
    setBtn.Font = Enum.Font.GothamBold
    setBtn.BorderSizePixel = 0
    setBtn.Parent = row
    Instance.new("UICorner", setBtn).CornerRadius = UDim.new(0, 4)
    setBtn.MouseButton1Click:Connect(function()
        SetAnimationBundle(bundleName)
        setBtn.Text = "Active!"
        setBtn.BackgroundColor3 = Color3.fromRGB(80, 200, 80)
        task.wait(1)
        setBtn.Text = "SET"
        setBtn.BackgroundColor3 = Theme.Accent
    end)
end

CreateButton(animSec, "Reset to Default Animations", function()
    currentAnimBundle = "Mocap"
    SetAnimationBundle("Mocap")
end)

local configTab = CreateTab("Config")
local configSec = CreateSection(configTab, "Configuration Manager")

local configNameInput = CreateTextbox(configSec, "Config Name", "default")

configStatusLabel = Instance.new("TextLabel")
configStatusLabel.Size = UDim2.new(1, 0, 0, 20)
configStatusLabel.BackgroundTransparency = 1
configStatusLabel.Text = "Status: " .. configStatusText
configStatusLabel.TextColor3 = Theme.TextDim
configStatusLabel.TextSize = 11
configStatusLabel.Font = Enum.Font.Gotham
configStatusLabel.TextXAlignment = Enum.TextXAlignment.Left
configStatusLabel.Parent = configSec

CreateButton(configSec, "Save Current Config", function()
    local name = configNameInput.getValue()
    if name and name ~= "" then saveConfig(name) end
end)

configListContainer = Instance.new("Frame")
configListContainer.Size = UDim2.new(1, 0, 0, 0)
configListContainer.BackgroundTransparency = 1
configListContainer.AutomaticSize = Enum.AutomaticSize.Y
configListContainer.Parent = configSec

local configLayout = Instance.new("UIListLayout")
configLayout.Padding = UDim.new(0, 6)
configLayout.SortOrder = Enum.SortOrder.LayoutOrder
configLayout.Parent = configListContainer

refreshConfigList()
renderConfigTable()

local settingsTab = CreateTab("Settings")
local setSec = CreateSection(settingsTab, "Menu Settings")

CreateToggle(setSec, "Menu Keybind", false, Enum.KeyCode.K, function(v) end, function(k) 
    if k then 
        menuToggleKey = k 
    end 
end)

CreateToggle(setSec, "Stealth Mode (Hides Minimize Icon)", false, nil, function(v)
    stealthMode = v
    if not ScreenGui.Enabled then
        MinimizeIcon.Visible = not v
    end
end)

CreateToggle(setSec, "Anti-AFK", false, nil, function(v) antiAFK = v end)
CreateToggle(setSec, "Fullbright", false, nil, function(v) fullbright = v; updateLighting() end)
CreateButton(setSec, "Rejoin Server", function() TeleportService:Teleport(game.PlaceId, player) end)
CreateButton(setSec, "Copy Username", function() if setclipboard then setclipboard(player.Name) end end)
CreateButton(setSec, "Destroy Script", function()
    if fovCircle then fovCircle:Remove() end
    for _, p in ipairs(Players:GetPlayers()) do cleanupESP(p) end
    activeESPs = {}
    fullbright = false; updateLighting()
    if flyConn then flyConn:Disconnect() end
    ContextActionService:UnbindAction("ToggleFuckerMenu")
    ScreenGui:Destroy()
end)

MinimizeBtn.MouseButton1Click:Connect(ToggleMenu)
CloseBtn.MouseButton1Click:Connect(function() 
    TweenService:Create(MainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.In), {Size = UDim2.new(0, 0, 0, 0), Position = UDim2.new(0.5, 0, 0.5, 0)}):Play()
    task.wait(0.3)
    ScreenGui.Enabled = false
    MinimizeIcon.Visible = not stealthMode
end)
MinimizeIcon.MouseButton1Click:Connect(ToggleMenu)

-- FIXED: Correctly references the "AimLock" tab name to prevent startup bugs
if #Tabs > 0 then
    local firstTab = Tabs["AimLock"]
    if firstTab then
        firstTab.Content.Visible = true
        firstTab.Btn.BackgroundTransparency = 0
        firstTab.Btn.BackgroundColor3 = Theme.Accent
        firstTab.Btn.TextColor3 = Theme.Text
        CurrentTab = "AimLock"
    end
end

applyMovement()
updateFly()
refreshConfigList()
renderConfigTable()
refreshPlayerList()

print("⛓ FUCKER // crymyslf [ULTIMATE GLASS EDITION] Loaded Successfully!")
print("Menu Toggle: K (changeable in Settings)")
